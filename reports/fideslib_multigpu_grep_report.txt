============================================================
FIDESlib multi-GPU / P2P / NCCL grep report
Generated at: Fri Apr 24 05:55:53 UTC 2026
FIDESLIB root: /root/fideslib
============================================================

============================================================
1) Check FIDESlib build artifacts for NCCL symbols
============================================================
[grep build logs]
/root/fideslib/build/CMakeFiles/fideslib.dir/flags.make:6:CUDA_DEFINES = -DMULTI_GPU -DNCCL -DRCCL
/root/fideslib/build/CMakeFiles/fideslib.dir/flags.make:10:CUDA_FLAGS =  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 "--generate-code=arch=compute_70,code=[sm_70]" "--generate-code=arch=compute_70,code=[compute_70]" "--generate-code=arch=compute_80,code=[sm_80]" "--generate-code=arch=compute_86,code=[sm_86]" "--generate-code=arch=compute_89,code=[sm_89]" "--generate-code=arch=compute_90,code=[sm_90]" "--generate-code=arch=compute_90,code=[compute_90]" "--generate-code=arch=compute_100,code=[sm_100]" "--generate-code=arch=compute_120,code=[sm_120]" -Xcompiler=-fPIC
/root/fideslib/build/CMakeFiles/fideslib.dir/flags.make:12:CXX_DEFINES = -DMULTI_GPU -DNCCL -DRCCL
/root/fideslib/build/CMakeFiles/fideslib.dir/flags.make:16:CXX_FLAGS =  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC
/root/fideslib/build/CMakeFiles/fideslib.dir/dlink.txt:1:/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG "--generate-code=arch=compute_70,code=[sm_70]" "--generate-code=arch=compute_70,code=[compute_70]" "--generate-code=arch=compute_80,code=[sm_80]" "--generate-code=arch=compute_86,code=[sm_86]" "--generate-code=arch=compute_89,code=[sm_89]" "--generate-code=arch=compute_90,code=[sm_90]" "--generate-code=arch=compute_90,code=[compute_90]" "--generate-code=arch=compute_100,code=[sm_100]" "--generate-code=arch=compute_120,code=[sm_120]" -Xcompiler=-fPIC -Wno-deprecated-gpu-targets -shared -dlink --options-file CMakeFiles/fideslib.dir/deviceObjects1.rsp -o CMakeFiles/fideslib.dir/cmake_device_link.o --options-file CMakeFiles/fideslib.dir/deviceLinkLibs.rsp
/root/fideslib/build/CMakeFiles/gpu-test.dir/flags.make:5:CUDA_DEFINES = -DMULTI_GPU -DNCCL -DRCCL
/root/fideslib/build/CMakeFiles/gpu-test.dir/flags.make:9:CUDA_FLAGS =  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 "--generate-code=arch=compute_70,code=[sm_70]" "--generate-code=arch=compute_70,code=[compute_70]" "--generate-code=arch=compute_80,code=[sm_80]" "--generate-code=arch=compute_86,code=[sm_86]" "--generate-code=arch=compute_89,code=[sm_89]" "--generate-code=arch=compute_90,code=[sm_90]" "--generate-code=arch=compute_90,code=[compute_90]" "--generate-code=arch=compute_100,code=[sm_100]" "--generate-code=arch=compute_120,code=[sm_120]" -Xcompiler=-fPIE
/root/fideslib/build/CMakeFiles/gpu-test.dir/dlink.txt:1:/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG "--generate-code=arch=compute_70,code=[sm_70]" "--generate-code=arch=compute_70,code=[compute_70]" "--generate-code=arch=compute_80,code=[sm_80]" "--generate-code=arch=compute_86,code=[sm_86]" "--generate-code=arch=compute_89,code=[sm_89]" "--generate-code=arch=compute_90,code=[sm_90]" "--generate-code=arch=compute_90,code=[compute_90]" "--generate-code=arch=compute_100,code=[sm_100]" "--generate-code=arch=compute_120,code=[sm_120]" -Xcompiler=-fPIC -Wno-deprecated-gpu-targets -shared -dlink --options-file CMakeFiles/gpu-test.dir/deviceObjects1.rsp -o CMakeFiles/gpu-test.dir/cmake_device_link.o --options-file CMakeFiles/gpu-test.dir/deviceLinkLibs.rsp
/root/fideslib/build/CMakeFiles/dummy.dir/flags.make:6:CUDA_DEFINES = -DMULTI_GPU -DNCCL -DRCCL
/root/fideslib/build/CMakeFiles/dummy.dir/flags.make:10:CUDA_FLAGS =  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 "--generate-code=arch=compute_70,code=[sm_70]" "--generate-code=arch=compute_70,code=[compute_70]" "--generate-code=arch=compute_80,code=[sm_80]" "--generate-code=arch=compute_86,code=[sm_86]" "--generate-code=arch=compute_89,code=[sm_89]" "--generate-code=arch=compute_90,code=[sm_90]" "--generate-code=arch=compute_90,code=[compute_90]" "--generate-code=arch=compute_100,code=[sm_100]" "--generate-code=arch=compute_120,code=[sm_120]" -Xcompiler=-fPIE
/root/fideslib/build/CMakeFiles/dummy.dir/flags.make:12:CXX_DEFINES = -DMULTI_GPU -DNCCL -DRCCL
/root/fideslib/build/CMakeFiles/dummy.dir/flags.make:16:CXX_FLAGS =  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIE
/root/fideslib/build/CMakeFiles/dummy.dir/link.txt:1:/usr/bin/g++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG CMakeFiles/dummy.dir/other/Dummy.cpp.o CMakeFiles/dummy.dir/cmake_device_link.o -o dummy   -L/usr/local/cuda/targets/x86_64-linux/lib  -Wl,-rpath,/usr/local/cuda-12.9/targets/x86_64-linux/lib fideslib.a /root/fideslib/deps/openfhe-install/lib/libOPENFHEpke_static.a /root/fideslib/deps/openfhe-install/lib/libOPENFHEbinfhe_static.a /root/fideslib/deps/openfhe-install/lib/libOPENFHEcore_static.a /usr/lib/x86_64-linux-gnu/libtbb.so.12.5 /usr/local/cuda-12.9/targets/x86_64-linux/lib/libcudart.so /usr/local/cuda/targets/x86_64-linux/lib/stubs/libcuda.so -ldl /usr/lib/x86_64-linux-gnu/librt.a -lnccl -lcudadevrt -lcudart_static -lrt -lpthread -ldl
/root/fideslib/build/CMakeFiles/dummy.dir/dlink.txt:1:/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG "--generate-code=arch=compute_70,code=[sm_70]" "--generate-code=arch=compute_70,code=[compute_70]" "--generate-code=arch=compute_80,code=[sm_80]" "--generate-code=arch=compute_86,code=[sm_86]" "--generate-code=arch=compute_89,code=[sm_89]" "--generate-code=arch=compute_90,code=[sm_90]" "--generate-code=arch=compute_90,code=[compute_90]" "--generate-code=arch=compute_100,code=[sm_100]" "--generate-code=arch=compute_120,code=[sm_120]" -Xcompiler=-fPIC -Wno-deprecated-gpu-targets -shared -dlink --options-file CMakeFiles/dummy.dir/deviceObjects1.rsp -o CMakeFiles/dummy.dir/cmake_device_link.o --options-file CMakeFiles/dummy.dir/deviceLinkLibs.rsp
/root/fideslib/build/compile_commands.json:4:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -I/root/fideslib/api -isystem /usr/local/cuda/targets/x86_64-linux/include -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC -o CMakeFiles/fideslib.dir/src/CKKS/Ciphertext.cpp.o -c /root/fideslib/src/CKKS/Ciphertext.cpp",
/root/fideslib/build/compile_commands.json:10:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -I/root/fideslib/api -isystem /usr/local/cuda/targets/x86_64-linux/include -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC -o CMakeFiles/fideslib.dir/src/CKKS/RNSPoly.cpp.o -c /root/fideslib/src/CKKS/RNSPoly.cpp",
/root/fideslib/build/compile_commands.json:16:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -I/root/fideslib/api -isystem /usr/local/cuda/targets/x86_64-linux/include -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC -o CMakeFiles/fideslib.dir/src/CKKS/RNSPolyBatch.cpp.o -c /root/fideslib/src/CKKS/RNSPolyBatch.cpp",
/root/fideslib/build/compile_commands.json:22:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -I/root/fideslib/api -isystem /usr/local/cuda/targets/x86_64-linux/include -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC -o CMakeFiles/fideslib.dir/src/parallel_for.cpp.o -c /root/fideslib/src/parallel_for.cpp",
/root/fideslib/build/compile_commands.json:28:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/AddSub.cu -o CMakeFiles/fideslib.dir/src/AddSub.cu.o",
/root/fideslib/build/compile_commands.json:34:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/AccumulateBroadcast.cu -o CMakeFiles/fideslib.dir/src/CKKS/AccumulateBroadcast.cu.o",
/root/fideslib/build/compile_commands.json:40:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/ApproxModEval.cu -o CMakeFiles/fideslib.dir/src/CKKS/ApproxModEval.cu.o",
/root/fideslib/build/compile_commands.json:46:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/Bootstrap.cu -o CMakeFiles/fideslib.dir/src/CKKS/Bootstrap.cu.o",
/root/fideslib/build/compile_commands.json:52:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/CoeffsToSlots.cu -o CMakeFiles/fideslib.dir/src/CKKS/CoeffsToSlots.cu.o",
/root/fideslib/build/compile_commands.json:58:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/Context.cu -o CMakeFiles/fideslib.dir/src/CKKS/Context.cu.o",
/root/fideslib/build/compile_commands.json:64:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/Conv.cu -o CMakeFiles/fideslib.dir/src/CKKS/Conv.cu.o",
/root/fideslib/build/compile_commands.json:70:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/ElemenwiseBatchKernels.cu -o CMakeFiles/fideslib.dir/src/CKKS/ElemenwiseBatchKernels.cu.o",
/root/fideslib/build/compile_commands.json:76:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/KeySwitchingKey.cu -o CMakeFiles/fideslib.dir/src/CKKS/KeySwitchingKey.cu.o",
/root/fideslib/build/compile_commands.json:82:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/Limb.cu -o CMakeFiles/fideslib.dir/src/CKKS/Limb.cu.o",
/root/fideslib/build/compile_commands.json:88:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/LimbPartition.cu -o CMakeFiles/fideslib.dir/src/CKKS/LimbPartition.cu.o",
/root/fideslib/build/compile_commands.json:94:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/LimbPartitionBatch.cu -o CMakeFiles/fideslib.dir/src/CKKS/LimbPartitionBatch.cu.o",
/root/fideslib/build/compile_commands.json:100:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/LimbPartitionMGPU.cu -o CMakeFiles/fideslib.dir/src/CKKS/LimbPartitionMGPU.cu.o",
/root/fideslib/build/compile_commands.json:106:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/LinearTransform.cu -o CMakeFiles/fideslib.dir/src/CKKS/LinearTransform.cu.o",
/root/fideslib/build/compile_commands.json:112:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/Parameters.cu -o CMakeFiles/fideslib.dir/src/CKKS/Parameters.cu.o",
/root/fideslib/build/compile_commands.json:118:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/Plaintext.cu -o CMakeFiles/fideslib.dir/src/CKKS/Plaintext.cu.o",
/root/fideslib/build/compile_commands.json:124:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/Rescale.cu -o CMakeFiles/fideslib.dir/src/CKKS/Rescale.cu.o",
/root/fideslib/build/compile_commands.json:130:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/openfhe-interface/ParameterSwitch.cu -o CMakeFiles/fideslib.dir/src/CKKS/openfhe-interface/ParameterSwitch.cu.o",
/root/fideslib/build/compile_commands.json:136:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CKKS/openfhe-interface/RawCiphertext.cu -o CMakeFiles/fideslib.dir/src/CKKS/openfhe-interface/RawCiphertext.cu.o",
/root/fideslib/build/compile_commands.json:142:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/ConstantsGPU.cu -o CMakeFiles/fideslib.dir/src/ConstantsGPU.cu.o",
/root/fideslib/build/compile_commands.json:148:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/CudaUtils.cu -o CMakeFiles/fideslib.dir/src/CudaUtils.cu.o",
/root/fideslib/build/compile_commands.json:154:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/Math.cu -o CMakeFiles/fideslib.dir/src/Math.cu.o",
/root/fideslib/build/compile_commands.json:160:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/ModMult.cu -o CMakeFiles/fideslib.dir/src/ModMult.cu.o",
/root/fideslib/build/compile_commands.json:166:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/NTT.cu -o CMakeFiles/fideslib.dir/src/NTT.cu.o",
/root/fideslib/build/compile_commands.json:172:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/PeerUtils.cu -o CMakeFiles/fideslib.dir/src/PeerUtils.cu.o",
/root/fideslib/build/compile_commands.json:178:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/PolyApprox.cu -o CMakeFiles/fideslib.dir/src/PolyApprox.cu.o",
/root/fideslib/build/compile_commands.json:184:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/Rotation.cu -o CMakeFiles/fideslib.dir/src/Rotation.cu.o",
/root/fideslib/build/compile_commands.json:190:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/fideslib.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIC -x cu -rdc=true -c /root/fideslib/src/VectorGPU.cu -o CMakeFiles/fideslib.dir/src/VectorGPU.cu.o",
/root/fideslib/build/compile_commands.json:196:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -I/root/fideslib/api -isystem /usr/local/cuda/targets/x86_64-linux/include -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC -o CMakeFiles/fideslib.dir/api/CCParams.cpp.o -c /root/fideslib/api/CCParams.cpp",
/root/fideslib/build/compile_commands.json:202:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -I/root/fideslib/api -isystem /usr/local/cuda/targets/x86_64-linux/include -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC -o CMakeFiles/fideslib.dir/api/Ciphertext.cpp.o -c /root/fideslib/api/Ciphertext.cpp",
/root/fideslib/build/compile_commands.json:208:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -I/root/fideslib/api -isystem /usr/local/cuda/targets/x86_64-linux/include -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC -o CMakeFiles/fideslib.dir/api/CryptoContext.cpp.o -c /root/fideslib/api/CryptoContext.cpp",
/root/fideslib/build/compile_commands.json:214:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -I/root/fideslib/api -isystem /usr/local/cuda/targets/x86_64-linux/include -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC -o CMakeFiles/fideslib.dir/api/GenCryptoContext.cpp.o -c /root/fideslib/api/GenCryptoContext.cpp",
/root/fideslib/build/compile_commands.json:220:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -I/root/fideslib/api -isystem /usr/local/cuda/targets/x86_64-linux/include -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC -o CMakeFiles/fideslib.dir/api/Plaintext.cpp.o -c /root/fideslib/api/Plaintext.cpp",
/root/fideslib/build/compile_commands.json:226:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -I/root/fideslib/api -isystem /usr/local/cuda/targets/x86_64-linux/include -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIC -o CMakeFiles/fideslib.dir/api/Serialize.cpp.o -c /root/fideslib/api/Serialize.cpp",
/root/fideslib/build/compile_commands.json:232:  "command": "/usr/local/cuda/bin/nvcc -forward-unknown-to-host-compiler -ccbin=/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL --options-file CMakeFiles/gpu-test.dir/includes_CUDA.rsp  -Wall -rdc=true --diag-suppress=3133,611 -Xcompiler -fopenmp -Xcompiler -O3 -Xcompiler -DNCCL -O3 -DNDEBUG -std=c++20 \"--generate-code=arch=compute_70,code=[sm_70]\" \"--generate-code=arch=compute_70,code=[compute_70]\" \"--generate-code=arch=compute_80,code=[sm_80]\" \"--generate-code=arch=compute_86,code=[sm_86]\" \"--generate-code=arch=compute_89,code=[sm_89]\" \"--generate-code=arch=compute_90,code=[sm_90]\" \"--generate-code=arch=compute_90,code=[compute_90]\" \"--generate-code=arch=compute_100,code=[sm_100]\" \"--generate-code=arch=compute_120,code=[sm_120]\" -Xcompiler=-fPIE -x cu -rdc=true -c /root/fideslib/other/GPUTest.cpp -o CMakeFiles/gpu-test.dir/other/GPUTest.cpp.o",
/root/fideslib/build/compile_commands.json:238:  "command": "/usr/bin/g++ -DMULTI_GPU -DNCCL -DRCCL -I/root/fideslib/deps/openfhe-install/include/openfhe -I/root/fideslib/deps/openfhe-install/include/openfhe/third-party/include -I/root/fideslib/deps/openfhe-install/include/openfhe/core -I/root/fideslib/deps/openfhe-install/include/openfhe/pke -I/root/fideslib/deps/openfhe-install/include/openfhe/binfhe -I/root/fideslib/src -I/root/fideslib/api -I/root/fideslib/DESTINATION -I/root/fideslib/include/fideslib -x c++  -Wall -fopenmp -DNCCL -O3 -DNDEBUG -std=gnu++20 -fPIE -o CMakeFiles/dummy.dir/other/Dummy.cpp.o -c /root/fideslib/other/Dummy.cpp",

[nm fideslib.a | grep nccl]
0000000000008190 T FIDESlib::CKKS::ContextData::PrepareNCCLCommunication()
0000000000000516 t FIDESlib::CKKS::ContextData::PrepareNCCLCommunication() [clone .cold]
0000000000000000 W std::vector<ncclComm*, std::allocator<ncclComm*> >::_M_default_append(unsigned long)
0000000000008000 t std::_Rb_tree_iterator<std::pair<int const, ncclComm**> > std::_Rb_tree<int, std::pair<int const, ncclComm**>, std::_Select1st<std::pair<int const, ncclComm**> >, std::less<int>, std::allocator<std::pair<int const, ncclComm**> > >::_M_emplace_hint_unique<std::piecewise_construct_t const&, std::tuple<int const&>, std::tuple<> >(std::_Rb_tree_const_iterator<std::pair<int const, ncclComm**> >, std::piecewise_construct_t const&, std::tuple<int const&>&&, std::tuple<>&&) [clone .isra.0]
0000000000000000 W std::_Rb_tree<int, std::pair<int const, ncclComm**>, std::_Select1st<std::pair<int const, ncclComm**> >, std::less<int>, std::allocator<std::pair<int const, ncclComm**> > >::_M_get_insert_unique_pos(int const&)
0000000000000000 t std::_Rb_tree<int, std::pair<int const, ncclComm**>, std::_Select1st<std::pair<int const, ncclComm**> >, std::less<int>, std::allocator<std::pair<int const, ncclComm**> > >::_M_erase(std::_Rb_tree_node<std::pair<int const, ncclComm**> >*) [clone .isra.0]
                 U ncclCommDeregister
                 U ncclCommDestroy
                 U ncclCommFinalize
                 U ncclCommInitRank
                 U ncclCommRegister
                 U ncclGetErrorString
                 U ncclGetUniqueId
                 U ncclGroupEnd
                 U ncclGroupStart
                 U ncclMemAlloc
                 U ncclMemFree
                 U ncclCommDeregister
                 U ncclCommRegister
                 U ncclGetErrorString
                 U ncclMemAlloc
                 U ncclMemFree
                 U ncclBroadcast
                 U ncclGetErrorString
                 U ncclGroupEnd
                 U ncclGroupStart
                 U ncclRecv
                 U ncclSend

============================================================
2) Source files containing GPU P2P print
============================================================
/root/fideslib/src/CKKS/Context.cu:709:        std::cout << "GPU P2P? " << this->canP2P << std::endl;
/root/fideslib/test/PeerUtilsTests.cu:741:    std::cout << "\n=== MULTI-GPU P2P BENCHMARK (with CUDA Events) ===" << std::endl;

============================================================
3) cudaDeviceCanAccessPeer occurrences
============================================================
/root/fideslib/src/CKKS/Context.cu:694:                    cudaDeviceCanAccessPeer(&canAccessPeer, i, j);
/root/fideslib/src/CKKS/Context.cu:700:                    cudaDeviceCanAccessPeer(&canAccessPeer, j, i);
/root/fideslib/test/PeerUtilsTests.cu:54:    CUDA_CHECK(cudaDeviceCanAccessPeer(&can_access, src_gpu, dst_gpu));
/root/fideslib/test/PeerUtilsTests.cu:429:    CUDA_CHECK(cudaDeviceCanAccessPeer(&canAccessSrcToDst, devSrc, devDst));
/root/fideslib/test/PeerUtilsTests.cu:430:    CUDA_CHECK(cudaDeviceCanAccessPeer(&canAccessDstToSrc, devDst, devSrc));
/root/fideslib/test/PeerUtilsTests.cu:455:    CUDA_CHECK(cudaDeviceCanAccessPeer(&canAccessSrcToDst, devSrc, devDst));
/root/fideslib/test/PeerUtilsTests.cu:456:    CUDA_CHECK(cudaDeviceCanAccessPeer(&canAccessDstToSrc, devDst, devSrc));
/root/fideslib/test/PeerUtilsTests.cu:481:    CUDA_CHECK(cudaDeviceCanAccessPeer(&canAccessSrcToDst, devSrc, devDst));
/root/fideslib/test/PeerUtilsTests.cu:482:    CUDA_CHECK(cudaDeviceCanAccessPeer(&canAccessDstToSrc, devDst, devSrc));
/root/fideslib/test/PeerUtilsTests.cu:727:    CUDA_CHECK(cudaDeviceCanAccessPeer(&canAccessSrcToDst, devSrc, devDst));
/root/fideslib/test/PeerUtilsTests.cu:728:    CUDA_CHECK(cudaDeviceCanAccessPeer(&canAccessDstToSrc, devDst, devSrc));
/root/fideslib/build/CMakeFiles/4.3.0/CompilerIdCUDA/tmp/CMakeCUDACompilerId.cudafe1.cpp:6294:extern cudaError_t cudaDeviceCanAccessPeer(int * canAccessPeer, int device, int peerDevice); 

============================================================
4) cudaDeviceEnablePeerAccess occurrences
============================================================
/root/fideslib/src/CKKS/Context.cu:696:                        cudaDeviceEnablePeerAccess(j, 0);
/root/fideslib/src/CKKS/Context.cu:702:                        cudaDeviceEnablePeerAccess(j, 0);
/root/fideslib/test/PeerUtilsTests.cu:63:    // Try to enable P2P; if already enabled, cudaDeviceEnablePeerAccess
/root/fideslib/test/PeerUtilsTests.cu:65:    cudaError_t err = cudaDeviceEnablePeerAccess(dst_gpu, 0);
/root/fideslib/build/CMakeFiles/4.3.0/CompilerIdCUDA/tmp/CMakeCUDACompilerId.cudafe1.cpp:6296:extern cudaError_t cudaDeviceEnablePeerAccess(int peerDevice, unsigned flags); 

============================================================
5) cudaMemcpyPeer / cudaMemcpyPeerAsync occurrences
============================================================
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:135:                        cudaMemcpyPeerAsync(buffer, cc.GPUid[i], cc.top_limb_buffer[top_gpu], cc.GPUid[top_gpu],
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:309:                                cudaMemcpyPeerAsync(buffer[0], cc.GPUid[i], cc.top_limb_buffer[top_gpu],
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:311:                                cudaMemcpyPeerAsync(buffer[1], cc.GPUid[i], cc.top_limb_buffer2[top_gpu],
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:959:                                                // cudaMemcpyPeerAsync(
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1301:                                            //   cudaMemcpyPeerAsync(
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1979:                                            //cudaMemcpyPeerAsync(bufferGather_[j] + cc.N * (start + start_d_i),
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2399:                                        cudaMemcpyPeerAsync(
/root/fideslib/src/PeerUtils.cu:776:        cudaMemcpyPeerAsync(dst, attr_dst.device, src, attr_src.device, elems * sizeof(float), s);
/root/fideslib/build/CMakeFiles/4.3.0/CompilerIdCUDA/tmp/CMakeCUDACompilerId.cudafe1.cpp:6194:extern cudaError_t cudaMemcpyPeer(void * dst, int dstDevice, const void * src, int srcDevice, size_t count); 
/root/fideslib/build/CMakeFiles/4.3.0/CompilerIdCUDA/tmp/CMakeCUDACompilerId.cudafe1.cpp:6210:extern cudaError_t cudaMemcpyPeerAsync(void * dst, int dstDevice, const void * src, int srcDevice, size_t count, cudaStream_t stream = 0); 

============================================================
6) NCCL API occurrences
============================================================
/root/fideslib/src/CudaUtils.cuh:93:        ncclResult_t res = cmd;                                                                     \
/root/fideslib/src/CudaUtils.cuh:94:        if (res != ncclSuccess) {                                                                   \
/root/fideslib/src/CudaUtils.cuh:95:            printf("Failed, NCCL error %s:%d '%s'\n", __FILE__, __LINE__, ncclGetErrorString(res)); \
/root/fideslib/src/CKKS/LimbPartition.cuh:14:#include "nccl.h"
/root/fideslib/src/CKKS/LimbPartition.cuh:28:    const ncclComm_t rank;  // For NCCL / RCCL
/root/fideslib/src/CKKS/Context.cu:719:        NCCLCHECK(ncclGetUniqueId(&communicatorID));
/root/fideslib/src/CKKS/Context.cu:721:        ncclGroupStart();
/root/fideslib/src/CKKS/Context.cu:725:                NCCLCHECK(ncclCommInitRank(GPUrank.data() + i, num_ranks, communicatorID, i));
/root/fideslib/src/CKKS/Context.cu:731:        ncclGroupEnd();
/root/fideslib/src/CKKS/Context.cu:751:            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer[i], sizeof(uint64_t) * N));
/root/fideslib/src/CKKS/Context.cu:753:                ncclCommRegister(GPUrank[g], top_limb_buffer[i], sizeof(uint64_t) * N, &top_limb_buffer_handle[i]));
/root/fideslib/src/CKKS/Context.cu:754:            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N));
/root/fideslib/src/CKKS/Context.cu:756:                ncclCommRegister(GPUrank[g], top_limb_buffer2[i], sizeof(uint64_t) * N, &top_limb_buffer2_handle[i]));
/root/fideslib/src/CKKS/Context.cu:862:                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer_handle[i]));
/root/fideslib/src/CKKS/Context.cu:864:                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer2_handle[i]));
/root/fideslib/src/CKKS/Context.cu:865:            NCCLCHECK(ncclMemFree(top_limb_buffer[i]));
/root/fideslib/src/CKKS/Context.cu:866:            NCCLCHECK(ncclMemFree(top_limb_buffer2[i]));
/root/fideslib/src/CKKS/Context.cu:883:    NCCLCHECK(ncclGroupStart());
/root/fideslib/src/CKKS/Context.cu:887:            NCCLCHECK(ncclCommFinalize(*rank.second));
/root/fideslib/src/CKKS/Context.cu:889:            NCCLCHECK(ncclCommDestroy(*rank.second));
/root/fideslib/src/CKKS/Context.cu:893:    NCCLCHECK(ncclGroupEnd());
/root/fideslib/src/CKKS/Context.cuh:19:#include "nccl.h"
/root/fideslib/src/CKKS/Context.cuh:31:    std::map<int, ncclComm_t*> dev_to_communicator;
/root/fideslib/src/CKKS/Context.cuh:76:    ncclUniqueId communicatorID;
/root/fideslib/src/CKKS/Context.cuh:77:    std::vector<ncclComm_t> GPUrank;
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:146:                NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr()));
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:148:                NCCLCHECK(ncclGroupStart());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:152:                            ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:155:                    ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:157:                NCCLCHECK(ncclGroupEnd());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:324:                NCCLCHECK(ncclGroupStart());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:328:                            ncclSend(buffer[0], cc.N, ncclUint64, i, rank, stream[0]->ptr());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:329:                            ncclSend(buffer[1], cc.N, ncclUint64, i, rank, stream[1]->ptr());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:333:                    ncclRecv(buffer[0], cc.N, ncclUint64, cc.limbGPUid[*part[0]->level].x, rank, stream[0]->ptr());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:334:                    ncclRecv(buffer[1], cc.N, ncclUint64, cc.limbGPUid[*part[1]->level].x, rank, stream[1]->ptr());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:336:                NCCLCHECK(ncclGroupEnd());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1031:                    NCCLCHECK(ncclGroupStart());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1052:                            NCCLCHECK(ncclBroadcast(
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1054:                                bufferGATHER + cc.N * (start + start_d_i), size_d_i * cc.N, ncclUint64, i, rank,
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1059:                    NCCLCHECK(ncclGroupEnd());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1367:                    NCCLCHECK(ncclGroupStart());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1382:                                ncclBroadcast(ptr, ptr, cc.N * num_limbs, ncclUint64, (int)j, rank, stream.ptr()));
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1385:                    NCCLCHECK(ncclGroupEnd());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2044:                    NCCLCHECK(ncclGroupStart());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2063:                            NCCLCHECK(ncclBroadcast(bufferGATHER + cc.N * (start + start_d_i),
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2065:                                                    ncclUint64, i, rank, stream.ptr()));
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2069:                    NCCLCHECK(ncclGroupEnd());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2440:                    NCCLCHECK(ncclGroupStart());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2450:                                ncclBroadcast(ptr, ptr, cc.N * num_limbs, ncclUint64, (int)i, rank, stream.ptr()));
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2453:                    NCCLCHECK(ncclGroupEnd());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2588:    { NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr())); }
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2595:        NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr()));
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2597:        NCCLCHECK(ncclGroupStart());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2601:                    ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2604:            ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr());
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2606:        NCCLCHECK(ncclGroupEnd());
/root/fideslib/src/CKKS/LimbPartition.cu:169:            NCCLCHECK(ncclCommDeregister(rank, bufferDECOMPandDIGIT_handle));
/root/fideslib/src/CKKS/LimbPartition.cu:170:        NCCLCHECK(ncclMemFree(bufferDECOMPandDIGIT));
/root/fideslib/src/CKKS/LimbPartition.cu:194:            NCCLCHECK(ncclCommDeregister(rank, bufferGATHER_handle));
/root/fideslib/src/CKKS/LimbPartition.cu:195:        NCCLCHECK(ncclMemFree(bufferGATHER));
/root/fideslib/src/CKKS/LimbPartition.cu:901:            NCCLCHECK(ncclMemAlloc((void**)&bufferDECOMPandDIGIT, std::max(1ul, size) * sizeof(uint64_t)));
/root/fideslib/src/CKKS/LimbPartition.cu:902:            NCCLCHECK(ncclCommRegister(rank, bufferDECOMPandDIGIT, std::max(1ul, size) * sizeof(uint64_t),
/root/fideslib/src/CKKS/LimbPartition.cu:2653:            NCCLCHECK(ncclMemAlloc((void**)&bufferGATHER, std::max(1ul, GATHERmeta.size() * sizeof(uint64_t) * cc.N)));
/root/fideslib/src/CKKS/LimbPartition.cu:2654:            NCCLCHECK(ncclCommRegister(rank, bufferGATHER, std::max(1ul, GATHERmeta.size() * sizeof(uint64_t) * cc.N),
/root/fideslib/src/PeerUtils.cu:67:            launchAttrs[attrs++].value.memSyncDomain = (CUlaunchMemSyncDomain) ncclParamMemSyncDomain();
/root/fideslib/test/PeerUtilsTests.cu:6:#include <nccl.h>
/root/fideslib/test/PeerUtilsTests.cu:773:                //ncclMemAlloc((void**)&chunks[i], chunk_size * 1024 * 1024);
/root/fideslib/test/PeerUtilsTests.cu:800:                //ncclMemFree(chunks[i]);
/root/fideslib/test/PeerUtilsTests.cu:845:            //ncclMemAlloc((void**)&chunks[i], chunk_size * 1024 * 1024);
/root/fideslib/test/PeerUtilsTests.cu:866:            //ncclMemFree(chunks[i]);

============================================================
7) Stream / synchronization related occurrences
============================================================
/root/fideslib/src/CudaUtils.cuh:55:        cudaDeviceSynchronize();                                                             \
/root/fideslib/src/CudaUtils.cuh:67:        cudaStreamSynchronize(0);                                                            \
/root/fideslib/src/CudaUtils.cuh:78:        /*cudaDeviceSynchronize();*/                                                                              \
/root/fideslib/src/CKKS/RNSPoly.cpp:248:            cudaStreamSynchronize(STREAM(j).ptr());
/root/fideslib/src/CKKS/RNSPoly.cpp:250:        cudaStreamSynchronize(i.s.ptr());
/root/fideslib/src/CKKS/RNSPoly.cpp:1041:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/RNSPoly.cpp:1049:                //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:27:	// cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:71:	// cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:74:	// cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:79:	// cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:83:	// cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:128:	// cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:473:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:509:	// cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:615:		cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:655:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:707:		cudaDeviceSynchronize();
/root/fideslib/src/CKKS/ApproxModEval.cu:723:		// cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Context.cu:733:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Context.cu:765:        //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Context.cu:1014:                //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Context.cu:1017:                //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:294:	cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:308:	cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:451:					cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:458:					cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:466:					cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:476:					cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:484:					cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:495:					cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:500:					cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:511:					cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:524:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:526:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:528:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:530:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:532:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:534:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:536:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:538:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:540:			cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:1087:					cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:1097:					cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:1179:				cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:1189:				cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:1760:		// cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Ciphertext.cpp:1762:		// cudaDeviceSynchronize();
/root/fideslib/src/CKKS/CoeffsToSlots.cu:80:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/CoeffsToSlots.cu:89:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/CoeffsToSlots.cu:102:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/CoeffsToSlots.cu:120:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/CoeffsToSlots.cu:126:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/CoeffsToSlots.cu:144:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/CoeffsToSlots.cu:168:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/CoeffsToSlots.cu:177:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:167:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:345:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:612:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:619:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:667:    //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:762:            //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:855:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:864:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1071:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1080:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1163:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1173:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1202:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1211:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1398:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1407:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1431:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1440:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1481:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1491:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1523:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1532:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1570:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1579:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1672:                //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1697:                //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1847:            //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1854:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1861:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1911:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1920:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2079:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2088:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2161:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2171:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2278:                //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2326:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2333:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2464:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2473:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2496:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2505:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2544:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2553:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:156:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:165:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:307:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:316:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:399:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:407:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:413:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:424:        cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:450:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:458:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:463:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:471:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:478:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:486:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:491:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:499:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:515:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:523:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:528:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:536:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:542:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:550:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:555:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Bootstrap.cu:563:                cudaDeviceSynchronize();
/root/fideslib/src/CKKS/KeySwitchingKey.cu:34:    cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Plaintext.cu:76:    cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Plaintext.cu:83:    cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Plaintext.cu:94:    cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Plaintext.cu:105:    cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Limb.cu:87:    cudaStreamSynchronize(stream.ptr());
/root/fideslib/src/CKKS/Limb.cu:88:    //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Limb.cu:110:    //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/Limb.cu:112:    //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:166:        cudaStreamSynchronize(s.ptr());
/root/fideslib/src/CKKS/LimbPartition.cu:190:        cudaStreamSynchronize(s.ptr());
/root/fideslib/src/CKKS/LimbPartition.cu:771:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:777:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:788:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:794:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:809:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:815:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:900:            cudaStreamSynchronize(s.ptr());
/root/fideslib/src/CKKS/LimbPartition.cu:906:            cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:1468:    //cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:2146:    cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:2156:    cudaDeviceSynchronize();
/root/fideslib/src/CKKS/LimbPartition.cu:2652:            cudaStreamSynchronize(s.ptr());
/root/fideslib/src/CKKS/LimbPartition.cu:2658:            cudaDeviceSynchronize();
/root/fideslib/src/VectorGPU.cu:42:    //cudaDeviceSynchronize();
/root/fideslib/src/VectorGPU.cu:70:        //cudaDeviceSynchronize();
/root/fideslib/src/VectorGPU.cu:72:        //cudaDeviceSynchronize();
/root/fideslib/src/CudaUtils.cu:93:    cudaDeviceSynchronize();
/root/fideslib/src/CudaUtils.cu:131:        cudaStreamWaitEvent(ptr, ev.ptr());
/root/fideslib/src/CudaUtils.cu:166:    cudaStreamSynchronize(0);
/root/fideslib/src/CudaUtils.cu:178:    cudaEventRecordWithFlags(ev, ptr_, external ? cudaEventRecordExternal : cudaEventRecordDefault);
/root/fideslib/src/CudaUtils.cu:188:    cudaStreamWaitEvent(ptr_, s.ev);
/root/fideslib/src/CudaUtils.cu:205:        cudaEventRecordWithFlags(s.ev, s.ptr_, cudaEventRecordDefault);
/root/fideslib/src/CudaUtils.cu:210:    cudaStreamWaitEvent(ptr_, s.ev, external ? cudaEventWaitExternal : cudaEventWaitDefault);
/root/fideslib/src/CudaUtils.cu:225:    cudaEventRecordWithFlags(ev, s, cudaEventRecordDefault);
/root/fideslib/src/CudaUtils.cu:230:    cudaStreamWaitEvent(ptr_, ev, cudaEventWaitDefault);
/root/fideslib/src/CudaUtils.cu:356:            cudaStreamWaitEvent(stream, s[id].ev);
/root/fideslib/src/CudaUtils.cu:433:        // cudaDeviceSynchronize();

============================================================
8) Likely multi-GPU source files
============================================================
/root/fideslib/src/CudaUtils.cuh
/root/fideslib/src/CKKS/Context.cu
/root/fideslib/src/CKKS/Context.cuh
/root/fideslib/src/CKKS/LimbPartitionMGPU.cu
/root/fideslib/src/PeerUtils.cuh
/root/fideslib/src/VectorGPU.cu
/root/fideslib/src/PeerUtils.cu
/root/fideslib/src/ConstantsGPU.cuh
/root/fideslib/src/VectorGPU.cuh
/root/fideslib/src/CudaUtils.cu
/root/fideslib/src/ConstantsGPU.cu

============================================================
9) Context.cu around GPU P2P / NCCL lines
============================================================

---------------- FILE: /root/fideslib/src/CudaUtils.cuh ----------------

----- /root/fideslib/src/CudaUtils.cuh:93 -----
        }                                                                                    \
    } while (0)

#define CudaCheckErrorModNoSync                                                                                   \
    do {                                                                                                          \
        /*cudaDeviceSynchronize();*/                                                                              \
        cudaError_t e = cudaGetLastError();                                                                       \
        if (e != cudaSuccess && e != cudaErrorPeerAccessAlreadyEnabled && e != cudaErrorGraphExecUpdateFailure) { \
            void* array[10];                                                                                      \
            size_t size;                                                                                          \
            size = backtrace(array, 10);                                                                          \
            backtrace_symbols_fd(array, size, STDERR_FILENO);                                                     \
            printf("Cuda failure %s:%d: '%s'\n", __FILE__, __LINE__, cudaGetErrorString(e));                      \
            FIDESlib::breakpoint();                                                                               \
            exit(0);                                                                                              \
        }                                                                                                         \
    } while (0)

#define NCCLCHECK(cmd)                                                                              \
    do {                                                                                            \
        ncclResult_t res = cmd;                                                                     \
        if (res != ncclSuccess) {                                                                   \
            printf("Failed, NCCL error %s:%d '%s'\n", __FILE__, __LINE__, ncclGetErrorString(res)); \
            exit(EXIT_FAILURE);                                                                     \
        }                                                                                           \
    } while (0)

class Event;

extern std::map<void*, int> free;

class Stream {
   private:
    cudaStream_t ptr_ = nullptr;

   public:
    cudaEvent_t ev = nullptr;
    bool updated = false;
    //Event ev;

    void init(int priority = 0);

----- /root/fideslib/src/CudaUtils.cuh:94 -----
    } while (0)

#define CudaCheckErrorModNoSync                                                                                   \
    do {                                                                                                          \
        /*cudaDeviceSynchronize();*/                                                                              \
        cudaError_t e = cudaGetLastError();                                                                       \
        if (e != cudaSuccess && e != cudaErrorPeerAccessAlreadyEnabled && e != cudaErrorGraphExecUpdateFailure) { \
            void* array[10];                                                                                      \
            size_t size;                                                                                          \
            size = backtrace(array, 10);                                                                          \
            backtrace_symbols_fd(array, size, STDERR_FILENO);                                                     \
            printf("Cuda failure %s:%d: '%s'\n", __FILE__, __LINE__, cudaGetErrorString(e));                      \
            FIDESlib::breakpoint();                                                                               \
            exit(0);                                                                                              \
        }                                                                                                         \
    } while (0)

#define NCCLCHECK(cmd)                                                                              \
    do {                                                                                            \
        ncclResult_t res = cmd;                                                                     \
        if (res != ncclSuccess) {                                                                   \
            printf("Failed, NCCL error %s:%d '%s'\n", __FILE__, __LINE__, ncclGetErrorString(res)); \
            exit(EXIT_FAILURE);                                                                     \
        }                                                                                           \
    } while (0)

class Event;

extern std::map<void*, int> free;

class Stream {
   private:
    cudaStream_t ptr_ = nullptr;

   public:
    cudaEvent_t ev = nullptr;
    bool updated = false;
    //Event ev;

    void init(int priority = 0);


----- /root/fideslib/src/CudaUtils.cuh:95 -----

#define CudaCheckErrorModNoSync                                                                                   \
    do {                                                                                                          \
        /*cudaDeviceSynchronize();*/                                                                              \
        cudaError_t e = cudaGetLastError();                                                                       \
        if (e != cudaSuccess && e != cudaErrorPeerAccessAlreadyEnabled && e != cudaErrorGraphExecUpdateFailure) { \
            void* array[10];                                                                                      \
            size_t size;                                                                                          \
            size = backtrace(array, 10);                                                                          \
            backtrace_symbols_fd(array, size, STDERR_FILENO);                                                     \
            printf("Cuda failure %s:%d: '%s'\n", __FILE__, __LINE__, cudaGetErrorString(e));                      \
            FIDESlib::breakpoint();                                                                               \
            exit(0);                                                                                              \
        }                                                                                                         \
    } while (0)

#define NCCLCHECK(cmd)                                                                              \
    do {                                                                                            \
        ncclResult_t res = cmd;                                                                     \
        if (res != ncclSuccess) {                                                                   \
            printf("Failed, NCCL error %s:%d '%s'\n", __FILE__, __LINE__, ncclGetErrorString(res)); \
            exit(EXIT_FAILURE);                                                                     \
        }                                                                                           \
    } while (0)

class Event;

extern std::map<void*, int> free;

class Stream {
   private:
    cudaStream_t ptr_ = nullptr;

   public:
    cudaEvent_t ev = nullptr;
    bool updated = false;
    //Event ev;

    void init(int priority = 0);

    cudaStream_t ptr() {

---------------- FILE: /root/fideslib/src/CKKS/LimbPartition.cuh ----------------

----- /root/fideslib/src/CKKS/LimbPartition.cuh:14 -----
//
// Created by carlosad on 16/03/24.
//

#ifndef FIDESLIB_CKKS_LIMBPARTITION_CUH
#define FIDESLIB_CKKS_LIMBPARTITION_CUH

#include <atomic>
#include "Limb.cuh"
#include "LimbUtils.cuh"
#include "NTT.cuh"
#include "PeerUtils.cuh"
#ifdef NCCL
#include "nccl.h"
#endif
namespace FIDESlib::CKKS {

extern bool MEMCPY_PEER;
extern bool GRAPH_CAPTURE;
class LimbPartition {
   public:
    ContextData& cc;
    const uint64_t uid;
    int* level;
    const int id;
    const int device;
#ifdef NCCL
    const ncclComm_t rank;  // For NCCL / RCCL
#else
    const int rank;
#endif
    Stream s;

    std::vector<LimbRecord>& meta;

----- /root/fideslib/src/CKKS/LimbPartition.cuh:28 -----
#include <atomic>
#include "Limb.cuh"
#include "LimbUtils.cuh"
#include "NTT.cuh"
#include "PeerUtils.cuh"
#ifdef NCCL
#include "nccl.h"
#endif
namespace FIDESlib::CKKS {

extern bool MEMCPY_PEER;
extern bool GRAPH_CAPTURE;
class LimbPartition {
   public:
    ContextData& cc;
    const uint64_t uid;
    int* level;
    const int id;
    const int device;
#ifdef NCCL
    const ncclComm_t rank;  // For NCCL / RCCL
#else
    const int rank;
#endif
    Stream s;

    std::vector<LimbRecord>& meta;
    std::vector<LimbRecord>& SPECIALmeta;
    const std::vector<int>& digitid;
    std::vector<std::vector<LimbRecord>>& DECOMPmeta;
    std::vector<std::vector<LimbRecord>>& DIGITmeta;
    std::vector<LimbRecord>& GATHERmeta;

    std::vector<LimbImpl> limb;
    std::vector<LimbImpl> SPECIALlimb;
    std::vector<std::vector<LimbImpl>> DECOMPlimb;
    std::vector<std::vector<LimbImpl>> DIGITlimb;
    std::vector<LimbImpl> GATHERlimb;

    void** bufferAUXptrs;
    VectorGPU<void*> limbptr;

---------------- FILE: /root/fideslib/src/CKKS/Context.cu ----------------

----- /root/fideslib/src/CKKS/Context.cu:694 -----
           : technique == lbcrypto::ScalingTechnique::FLEXIBLEAUTOEXT ? FIDESlib::CKKS::FLEXIBLEAUTOEXT
           : technique == lbcrypto::ScalingTechnique::FLEXIBLEAUTO    ? FIDESlib::CKKS::FLEXIBLEAUTO
                                                                      : FIDESlib::CKKS::NO_RESCALE;
}

void ContextData::PrepareNCCLCommunication() {

    if (GPUid.size() > 1) {

        std::set<int> ids;
        for (int i : GPUid)
            ids.insert(i);
        int num_ranks = ids.size();

        bool p2p = true;
        for (auto& i : ids) {
            cudaSetDevice(i);
            for (auto& j : ids) {
                if (i != j) {
                    int canAccessPeer;
                    cudaDeviceCanAccessPeer(&canAccessPeer, i, j);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;

                    cudaDeviceCanAccessPeer(&canAccessPeer, j, i);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;
                }
            }
        }
        this->canP2P = p2p;
        std::cout << "GPU P2P? " << this->canP2P << std::endl;

        /*
        if (GPUid.size() > 1) {
            if (this->canP2P)
                std::cout << "P2P (Nvlink?) detected" << std::endl;

----- /root/fideslib/src/CKKS/Context.cu:696 -----
                                                                      : FIDESlib::CKKS::NO_RESCALE;
}

void ContextData::PrepareNCCLCommunication() {

    if (GPUid.size() > 1) {

        std::set<int> ids;
        for (int i : GPUid)
            ids.insert(i);
        int num_ranks = ids.size();

        bool p2p = true;
        for (auto& i : ids) {
            cudaSetDevice(i);
            for (auto& j : ids) {
                if (i != j) {
                    int canAccessPeer;
                    cudaDeviceCanAccessPeer(&canAccessPeer, i, j);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;

                    cudaDeviceCanAccessPeer(&canAccessPeer, j, i);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;
                }
            }
        }
        this->canP2P = p2p;
        std::cout << "GPU P2P? " << this->canP2P << std::endl;

        /*
        if (GPUid.size() > 1) {
            if (this->canP2P)
                std::cout << "P2P (Nvlink?) detected" << std::endl;
            else
                std::cout << "NO P2P" << std::endl;

----- /root/fideslib/src/CKKS/Context.cu:700 -----

    if (GPUid.size() > 1) {

        std::set<int> ids;
        for (int i : GPUid)
            ids.insert(i);
        int num_ranks = ids.size();

        bool p2p = true;
        for (auto& i : ids) {
            cudaSetDevice(i);
            for (auto& j : ids) {
                if (i != j) {
                    int canAccessPeer;
                    cudaDeviceCanAccessPeer(&canAccessPeer, i, j);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;

                    cudaDeviceCanAccessPeer(&canAccessPeer, j, i);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;
                }
            }
        }
        this->canP2P = p2p;
        std::cout << "GPU P2P? " << this->canP2P << std::endl;

        /*
        if (GPUid.size() > 1) {
            if (this->canP2P)
                std::cout << "P2P (Nvlink?) detected" << std::endl;
            else
                std::cout << "NO P2P" << std::endl;
        }*/
#ifdef NCCL
        NCCLCHECK(ncclGetUniqueId(&communicatorID));
        GPUrank.resize(GPUid.size());

----- /root/fideslib/src/CKKS/Context.cu:702 -----

        std::set<int> ids;
        for (int i : GPUid)
            ids.insert(i);
        int num_ranks = ids.size();

        bool p2p = true;
        for (auto& i : ids) {
            cudaSetDevice(i);
            for (auto& j : ids) {
                if (i != j) {
                    int canAccessPeer;
                    cudaDeviceCanAccessPeer(&canAccessPeer, i, j);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;

                    cudaDeviceCanAccessPeer(&canAccessPeer, j, i);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;
                }
            }
        }
        this->canP2P = p2p;
        std::cout << "GPU P2P? " << this->canP2P << std::endl;

        /*
        if (GPUid.size() > 1) {
            if (this->canP2P)
                std::cout << "P2P (Nvlink?) detected" << std::endl;
            else
                std::cout << "NO P2P" << std::endl;
        }*/
#ifdef NCCL
        NCCLCHECK(ncclGetUniqueId(&communicatorID));
        GPUrank.resize(GPUid.size());
        ncclGroupStart();
        for (int i = 0; i < GPUid.size(); i++) {

----- /root/fideslib/src/CKKS/Context.cu:709 -----
        for (auto& i : ids) {
            cudaSetDevice(i);
            for (auto& j : ids) {
                if (i != j) {
                    int canAccessPeer;
                    cudaDeviceCanAccessPeer(&canAccessPeer, i, j);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;

                    cudaDeviceCanAccessPeer(&canAccessPeer, j, i);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;
                }
            }
        }
        this->canP2P = p2p;
        std::cout << "GPU P2P? " << this->canP2P << std::endl;

        /*
        if (GPUid.size() > 1) {
            if (this->canP2P)
                std::cout << "P2P (Nvlink?) detected" << std::endl;
            else
                std::cout << "NO P2P" << std::endl;
        }*/
#ifdef NCCL
        NCCLCHECK(ncclGetUniqueId(&communicatorID));
        GPUrank.resize(GPUid.size());
        ncclGroupStart();
        for (int i = 0; i < GPUid.size(); i++) {
            cudaSetDevice(GPUid[i]);
            if (precom.dev_to_communicator[GPUid[i]] == nullptr) {
                NCCLCHECK(ncclCommInitRank(GPUrank.data() + i, num_ranks, communicatorID, i));
                precom.dev_to_communicator[GPUid[i]] = GPUrank.data() + i;
            } else {
                GPUrank[i] = *precom.dev_to_communicator[GPUid[i]];
            }

----- /root/fideslib/src/CKKS/Context.cu:719 -----

                    cudaDeviceCanAccessPeer(&canAccessPeer, j, i);
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;
                }
            }
        }
        this->canP2P = p2p;
        std::cout << "GPU P2P? " << this->canP2P << std::endl;

        /*
        if (GPUid.size() > 1) {
            if (this->canP2P)
                std::cout << "P2P (Nvlink?) detected" << std::endl;
            else
                std::cout << "NO P2P" << std::endl;
        }*/
#ifdef NCCL
        NCCLCHECK(ncclGetUniqueId(&communicatorID));
        GPUrank.resize(GPUid.size());
        ncclGroupStart();
        for (int i = 0; i < GPUid.size(); i++) {
            cudaSetDevice(GPUid[i]);
            if (precom.dev_to_communicator[GPUid[i]] == nullptr) {
                NCCLCHECK(ncclCommInitRank(GPUrank.data() + i, num_ranks, communicatorID, i));
                precom.dev_to_communicator[GPUid[i]] = GPUrank.data() + i;
            } else {
                GPUrank[i] = *precom.dev_to_communicator[GPUid[i]];
            }
        }
        ncclGroupEnd();
#endif
        cudaDeviceSynchronize();
    }

    top_limb_stream.resize(2 * GPUid.size());
    top_limb_stream2.resize(2 * GPUid.size());
    top_limb_buffer.resize(2 * GPUid.size());
    top_limb_buffer2.resize(2 * GPUid.size());

----- /root/fideslib/src/CKKS/Context.cu:721 -----
                    if (canAccessPeer)
                        cudaDeviceEnablePeerAccess(j, 0);
                    else
                        p2p = false;
                }
            }
        }
        this->canP2P = p2p;
        std::cout << "GPU P2P? " << this->canP2P << std::endl;

        /*
        if (GPUid.size() > 1) {
            if (this->canP2P)
                std::cout << "P2P (Nvlink?) detected" << std::endl;
            else
                std::cout << "NO P2P" << std::endl;
        }*/
#ifdef NCCL
        NCCLCHECK(ncclGetUniqueId(&communicatorID));
        GPUrank.resize(GPUid.size());
        ncclGroupStart();
        for (int i = 0; i < GPUid.size(); i++) {
            cudaSetDevice(GPUid[i]);
            if (precom.dev_to_communicator[GPUid[i]] == nullptr) {
                NCCLCHECK(ncclCommInitRank(GPUrank.data() + i, num_ranks, communicatorID, i));
                precom.dev_to_communicator[GPUid[i]] = GPUrank.data() + i;
            } else {
                GPUrank[i] = *precom.dev_to_communicator[GPUid[i]];
            }
        }
        ncclGroupEnd();
#endif
        cudaDeviceSynchronize();
    }

    top_limb_stream.resize(2 * GPUid.size());
    top_limb_stream2.resize(2 * GPUid.size());
    top_limb_buffer.resize(2 * GPUid.size());
    top_limb_buffer2.resize(2 * GPUid.size());
    top_limb_buffer_handle.resize(2 * GPUid.size());
    top_limb_buffer2_handle.resize(2 * GPUid.size());

----- /root/fideslib/src/CKKS/Context.cu:725 -----
                }
            }
        }
        this->canP2P = p2p;
        std::cout << "GPU P2P? " << this->canP2P << std::endl;

        /*
        if (GPUid.size() > 1) {
            if (this->canP2P)
                std::cout << "P2P (Nvlink?) detected" << std::endl;
            else
                std::cout << "NO P2P" << std::endl;
        }*/
#ifdef NCCL
        NCCLCHECK(ncclGetUniqueId(&communicatorID));
        GPUrank.resize(GPUid.size());
        ncclGroupStart();
        for (int i = 0; i < GPUid.size(); i++) {
            cudaSetDevice(GPUid[i]);
            if (precom.dev_to_communicator[GPUid[i]] == nullptr) {
                NCCLCHECK(ncclCommInitRank(GPUrank.data() + i, num_ranks, communicatorID, i));
                precom.dev_to_communicator[GPUid[i]] = GPUrank.data() + i;
            } else {
                GPUrank[i] = *precom.dev_to_communicator[GPUid[i]];
            }
        }
        ncclGroupEnd();
#endif
        cudaDeviceSynchronize();
    }

    top_limb_stream.resize(2 * GPUid.size());
    top_limb_stream2.resize(2 * GPUid.size());
    top_limb_buffer.resize(2 * GPUid.size());
    top_limb_buffer2.resize(2 * GPUid.size());
    top_limb_buffer_handle.resize(2 * GPUid.size());
    top_limb_buffer2_handle.resize(2 * GPUid.size());
    for (size_t i = 0; i < 2 * GPUid.size(); ++i) {
        int g = i % GPUid.size();
        cudaSetDevice(GPUid[g]);


----- /root/fideslib/src/CKKS/Context.cu:731 -----
        /*
        if (GPUid.size() > 1) {
            if (this->canP2P)
                std::cout << "P2P (Nvlink?) detected" << std::endl;
            else
                std::cout << "NO P2P" << std::endl;
        }*/
#ifdef NCCL
        NCCLCHECK(ncclGetUniqueId(&communicatorID));
        GPUrank.resize(GPUid.size());
        ncclGroupStart();
        for (int i = 0; i < GPUid.size(); i++) {
            cudaSetDevice(GPUid[i]);
            if (precom.dev_to_communicator[GPUid[i]] == nullptr) {
                NCCLCHECK(ncclCommInitRank(GPUrank.data() + i, num_ranks, communicatorID, i));
                precom.dev_to_communicator[GPUid[i]] = GPUrank.data() + i;
            } else {
                GPUrank[i] = *precom.dev_to_communicator[GPUid[i]];
            }
        }
        ncclGroupEnd();
#endif
        cudaDeviceSynchronize();
    }

    top_limb_stream.resize(2 * GPUid.size());
    top_limb_stream2.resize(2 * GPUid.size());
    top_limb_buffer.resize(2 * GPUid.size());
    top_limb_buffer2.resize(2 * GPUid.size());
    top_limb_buffer_handle.resize(2 * GPUid.size());
    top_limb_buffer2_handle.resize(2 * GPUid.size());
    for (size_t i = 0; i < 2 * GPUid.size(); ++i) {
        int g = i % GPUid.size();
        cudaSetDevice(GPUid[g]);

        top_limb_stream[g].init(100);
        top_limb_stream2[g].init(100);

        if (GPUid.size() > 1) {
#ifdef NCCL
            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer[i], sizeof(uint64_t) * N));

----- /root/fideslib/src/CKKS/Context.cu:751 -----
        ncclGroupEnd();
#endif
        cudaDeviceSynchronize();
    }

    top_limb_stream.resize(2 * GPUid.size());
    top_limb_stream2.resize(2 * GPUid.size());
    top_limb_buffer.resize(2 * GPUid.size());
    top_limb_buffer2.resize(2 * GPUid.size());
    top_limb_buffer_handle.resize(2 * GPUid.size());
    top_limb_buffer2_handle.resize(2 * GPUid.size());
    for (size_t i = 0; i < 2 * GPUid.size(); ++i) {
        int g = i % GPUid.size();
        cudaSetDevice(GPUid[g]);

        top_limb_stream[g].init(100);
        top_limb_stream2[g].init(100);

        if (GPUid.size() > 1) {
#ifdef NCCL
            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer[i], sizeof(uint64_t) * N));
            NCCLCHECK(
                ncclCommRegister(GPUrank[g], top_limb_buffer[i], sizeof(uint64_t) * N, &top_limb_buffer_handle[i]));
            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N));
            NCCLCHECK(
                ncclCommRegister(GPUrank[g], top_limb_buffer2[i], sizeof(uint64_t) * N, &top_limb_buffer2_handle[i]));
#else
            cudaMalloc((void**)&top_limb_buffer[i], sizeof(uint63_t) * N);
            cudaMalloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N);
#endif
        } else {
            cudaMalloc((void**)&top_limb_buffer[i], sizeof(uint64_t) * N);
            cudaMalloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N);
        }
        //cudaDeviceSynchronize();
        top_limbptr.emplace_back(top_limb_stream[g], 1, GPUid[g], (void**)&top_limb_buffer[i]);
        top_limbptr2.emplace_back(top_limb_stream2[g], 1, GPUid[g], (void**)&top_limb_buffer2[i]);
        gatherStream.resize(GPUid.size());

        for (size_t i = 0; i < GPUid.size(); ++i) {
            gatherStream[i].resize(GPUid.size());

----- /root/fideslib/src/CKKS/Context.cu:753 -----
        cudaDeviceSynchronize();
    }

    top_limb_stream.resize(2 * GPUid.size());
    top_limb_stream2.resize(2 * GPUid.size());
    top_limb_buffer.resize(2 * GPUid.size());
    top_limb_buffer2.resize(2 * GPUid.size());
    top_limb_buffer_handle.resize(2 * GPUid.size());
    top_limb_buffer2_handle.resize(2 * GPUid.size());
    for (size_t i = 0; i < 2 * GPUid.size(); ++i) {
        int g = i % GPUid.size();
        cudaSetDevice(GPUid[g]);

        top_limb_stream[g].init(100);
        top_limb_stream2[g].init(100);

        if (GPUid.size() > 1) {
#ifdef NCCL
            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer[i], sizeof(uint64_t) * N));
            NCCLCHECK(
                ncclCommRegister(GPUrank[g], top_limb_buffer[i], sizeof(uint64_t) * N, &top_limb_buffer_handle[i]));
            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N));
            NCCLCHECK(
                ncclCommRegister(GPUrank[g], top_limb_buffer2[i], sizeof(uint64_t) * N, &top_limb_buffer2_handle[i]));
#else
            cudaMalloc((void**)&top_limb_buffer[i], sizeof(uint63_t) * N);
            cudaMalloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N);
#endif
        } else {
            cudaMalloc((void**)&top_limb_buffer[i], sizeof(uint64_t) * N);
            cudaMalloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N);
        }
        //cudaDeviceSynchronize();
        top_limbptr.emplace_back(top_limb_stream[g], 1, GPUid[g], (void**)&top_limb_buffer[i]);
        top_limbptr2.emplace_back(top_limb_stream2[g], 1, GPUid[g], (void**)&top_limb_buffer2[i]);
        gatherStream.resize(GPUid.size());

        for (size_t i = 0; i < GPUid.size(); ++i) {
            gatherStream[i].resize(GPUid.size());
            for (size_t j = 0; j < GPUid.size(); ++j) {
                cudaSetDevice(GPUid[j]);

----- /root/fideslib/src/CKKS/Context.cu:754 -----
    }

    top_limb_stream.resize(2 * GPUid.size());
    top_limb_stream2.resize(2 * GPUid.size());
    top_limb_buffer.resize(2 * GPUid.size());
    top_limb_buffer2.resize(2 * GPUid.size());
    top_limb_buffer_handle.resize(2 * GPUid.size());
    top_limb_buffer2_handle.resize(2 * GPUid.size());
    for (size_t i = 0; i < 2 * GPUid.size(); ++i) {
        int g = i % GPUid.size();
        cudaSetDevice(GPUid[g]);

        top_limb_stream[g].init(100);
        top_limb_stream2[g].init(100);

        if (GPUid.size() > 1) {
#ifdef NCCL
            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer[i], sizeof(uint64_t) * N));
            NCCLCHECK(
                ncclCommRegister(GPUrank[g], top_limb_buffer[i], sizeof(uint64_t) * N, &top_limb_buffer_handle[i]));
            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N));
            NCCLCHECK(
                ncclCommRegister(GPUrank[g], top_limb_buffer2[i], sizeof(uint64_t) * N, &top_limb_buffer2_handle[i]));
#else
            cudaMalloc((void**)&top_limb_buffer[i], sizeof(uint63_t) * N);
            cudaMalloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N);
#endif
        } else {
            cudaMalloc((void**)&top_limb_buffer[i], sizeof(uint64_t) * N);
            cudaMalloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N);
        }
        //cudaDeviceSynchronize();
        top_limbptr.emplace_back(top_limb_stream[g], 1, GPUid[g], (void**)&top_limb_buffer[i]);
        top_limbptr2.emplace_back(top_limb_stream2[g], 1, GPUid[g], (void**)&top_limb_buffer2[i]);
        gatherStream.resize(GPUid.size());

        for (size_t i = 0; i < GPUid.size(); ++i) {
            gatherStream[i].resize(GPUid.size());
            for (size_t j = 0; j < GPUid.size(); ++j) {
                cudaSetDevice(GPUid[j]);
                gatherStream[i][j].init(100);

----- /root/fideslib/src/CKKS/Context.cu:756 -----
    top_limb_stream.resize(2 * GPUid.size());
    top_limb_stream2.resize(2 * GPUid.size());
    top_limb_buffer.resize(2 * GPUid.size());
    top_limb_buffer2.resize(2 * GPUid.size());
    top_limb_buffer_handle.resize(2 * GPUid.size());
    top_limb_buffer2_handle.resize(2 * GPUid.size());
    for (size_t i = 0; i < 2 * GPUid.size(); ++i) {
        int g = i % GPUid.size();
        cudaSetDevice(GPUid[g]);

        top_limb_stream[g].init(100);
        top_limb_stream2[g].init(100);

        if (GPUid.size() > 1) {
#ifdef NCCL
            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer[i], sizeof(uint64_t) * N));
            NCCLCHECK(
                ncclCommRegister(GPUrank[g], top_limb_buffer[i], sizeof(uint64_t) * N, &top_limb_buffer_handle[i]));
            NCCLCHECK(ncclMemAlloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N));
            NCCLCHECK(
                ncclCommRegister(GPUrank[g], top_limb_buffer2[i], sizeof(uint64_t) * N, &top_limb_buffer2_handle[i]));
#else
            cudaMalloc((void**)&top_limb_buffer[i], sizeof(uint63_t) * N);
            cudaMalloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N);
#endif
        } else {
            cudaMalloc((void**)&top_limb_buffer[i], sizeof(uint64_t) * N);
            cudaMalloc((void**)&top_limb_buffer2[i], sizeof(uint64_t) * N);
        }
        //cudaDeviceSynchronize();
        top_limbptr.emplace_back(top_limb_stream[g], 1, GPUid[g], (void**)&top_limb_buffer[i]);
        top_limbptr2.emplace_back(top_limb_stream2[g], 1, GPUid[g], (void**)&top_limb_buffer2[i]);
        gatherStream.resize(GPUid.size());

        for (size_t i = 0; i < GPUid.size(); ++i) {
            gatherStream[i].resize(GPUid.size());
            for (size_t j = 0; j < GPUid.size(); ++j) {
                cudaSetDevice(GPUid[j]);
                gatherStream[i][j].init(100);
            }
        }

----- /root/fideslib/src/CKKS/Context.cu:862 -----
    key_switch_aux.reset(nullptr);
    //   CudaCheckErrorMod;
    key_switch_aux2.reset(nullptr);
    //   CudaCheckErrorMod;
    for (auto& i : moddown_aux) {
        i.reset(nullptr);
    }
    //   CudaCheckErrorMod;
    precom.auxPoly.clear();
    //   CudaCheckErrorMod;
    precom.monomialCache.clear();
    //   CudaCheckErrorMod;
    precom.boot.clear();
    //   CudaCheckErrorMod;
    for (size_t i = 0; i < top_limbptr.size(); ++i) {
        int g = i % GPUid.size();
        cudaSetDevice(GPUid[g]);
        if (GPUid.size() > 1) {
#ifdef NCCL
            if (top_limb_buffer_handle[i])
                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer_handle[i]));
            if (top_limb_buffer2_handle[i])
                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer2_handle[i]));
            NCCLCHECK(ncclMemFree(top_limb_buffer[i]));
            NCCLCHECK(ncclMemFree(top_limb_buffer2[i]));
#else

            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
#endif
        } else {
            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
        }
        top_limbptr[i].free(top_limb_stream[g]);
        top_limbptr2[i].free(top_limb_stream2[g]);
    }
    top_limbptr.clear();
    top_limbptr2.clear();
    CudaCheckErrorMod;
#ifdef NCCL

----- /root/fideslib/src/CKKS/Context.cu:864 -----
    key_switch_aux2.reset(nullptr);
    //   CudaCheckErrorMod;
    for (auto& i : moddown_aux) {
        i.reset(nullptr);
    }
    //   CudaCheckErrorMod;
    precom.auxPoly.clear();
    //   CudaCheckErrorMod;
    precom.monomialCache.clear();
    //   CudaCheckErrorMod;
    precom.boot.clear();
    //   CudaCheckErrorMod;
    for (size_t i = 0; i < top_limbptr.size(); ++i) {
        int g = i % GPUid.size();
        cudaSetDevice(GPUid[g]);
        if (GPUid.size() > 1) {
#ifdef NCCL
            if (top_limb_buffer_handle[i])
                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer_handle[i]));
            if (top_limb_buffer2_handle[i])
                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer2_handle[i]));
            NCCLCHECK(ncclMemFree(top_limb_buffer[i]));
            NCCLCHECK(ncclMemFree(top_limb_buffer2[i]));
#else

            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
#endif
        } else {
            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
        }
        top_limbptr[i].free(top_limb_stream[g]);
        top_limbptr2[i].free(top_limb_stream2[g]);
    }
    top_limbptr.clear();
    top_limbptr2.clear();
    CudaCheckErrorMod;
#ifdef NCCL
    NCCLCHECK(ncclGroupStart());
    for (auto rank : precom.dev_to_communicator) {

----- /root/fideslib/src/CKKS/Context.cu:865 -----
    //   CudaCheckErrorMod;
    for (auto& i : moddown_aux) {
        i.reset(nullptr);
    }
    //   CudaCheckErrorMod;
    precom.auxPoly.clear();
    //   CudaCheckErrorMod;
    precom.monomialCache.clear();
    //   CudaCheckErrorMod;
    precom.boot.clear();
    //   CudaCheckErrorMod;
    for (size_t i = 0; i < top_limbptr.size(); ++i) {
        int g = i % GPUid.size();
        cudaSetDevice(GPUid[g]);
        if (GPUid.size() > 1) {
#ifdef NCCL
            if (top_limb_buffer_handle[i])
                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer_handle[i]));
            if (top_limb_buffer2_handle[i])
                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer2_handle[i]));
            NCCLCHECK(ncclMemFree(top_limb_buffer[i]));
            NCCLCHECK(ncclMemFree(top_limb_buffer2[i]));
#else

            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
#endif
        } else {
            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
        }
        top_limbptr[i].free(top_limb_stream[g]);
        top_limbptr2[i].free(top_limb_stream2[g]);
    }
    top_limbptr.clear();
    top_limbptr2.clear();
    CudaCheckErrorMod;
#ifdef NCCL
    NCCLCHECK(ncclGroupStart());
    for (auto rank : precom.dev_to_communicator) {
        if (rank.second) {

----- /root/fideslib/src/CKKS/Context.cu:866 -----
    for (auto& i : moddown_aux) {
        i.reset(nullptr);
    }
    //   CudaCheckErrorMod;
    precom.auxPoly.clear();
    //   CudaCheckErrorMod;
    precom.monomialCache.clear();
    //   CudaCheckErrorMod;
    precom.boot.clear();
    //   CudaCheckErrorMod;
    for (size_t i = 0; i < top_limbptr.size(); ++i) {
        int g = i % GPUid.size();
        cudaSetDevice(GPUid[g]);
        if (GPUid.size() > 1) {
#ifdef NCCL
            if (top_limb_buffer_handle[i])
                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer_handle[i]));
            if (top_limb_buffer2_handle[i])
                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer2_handle[i]));
            NCCLCHECK(ncclMemFree(top_limb_buffer[i]));
            NCCLCHECK(ncclMemFree(top_limb_buffer2[i]));
#else

            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
#endif
        } else {
            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
        }
        top_limbptr[i].free(top_limb_stream[g]);
        top_limbptr2[i].free(top_limb_stream2[g]);
    }
    top_limbptr.clear();
    top_limbptr2.clear();
    CudaCheckErrorMod;
#ifdef NCCL
    NCCLCHECK(ncclGroupStart());
    for (auto rank : precom.dev_to_communicator) {
        if (rank.second) {
            cudaSetDevice(rank.first);

----- /root/fideslib/src/CKKS/Context.cu:883 -----
            if (top_limb_buffer2_handle[i])
                NCCLCHECK(ncclCommDeregister(GPUrank[g], top_limb_buffer2_handle[i]));
            NCCLCHECK(ncclMemFree(top_limb_buffer[i]));
            NCCLCHECK(ncclMemFree(top_limb_buffer2[i]));
#else

            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
#endif
        } else {
            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
        }
        top_limbptr[i].free(top_limb_stream[g]);
        top_limbptr2[i].free(top_limb_stream2[g]);
    }
    top_limbptr.clear();
    top_limbptr2.clear();
    CudaCheckErrorMod;
#ifdef NCCL
    NCCLCHECK(ncclGroupStart());
    for (auto rank : precom.dev_to_communicator) {
        if (rank.second) {
            cudaSetDevice(rank.first);
            NCCLCHECK(ncclCommFinalize(*rank.second));
            // CudaCheckErrorMod;
            NCCLCHECK(ncclCommDestroy(*rank.second));
            // CudaCheckErrorMod;
        }
    }
    NCCLCHECK(ncclGroupEnd());
#endif
    Ciphertext::clearOpRecord();
    CudaCheckErrorMod;
}

bool ContextData::hasAuxilarPoly() const {
    return precom.auxPoly.empty();
}

RNSPoly ContextData::getAuxilarPoly() {

----- /root/fideslib/src/CKKS/Context.cu:887 -----
#else

            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
#endif
        } else {
            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
        }
        top_limbptr[i].free(top_limb_stream[g]);
        top_limbptr2[i].free(top_limb_stream2[g]);
    }
    top_limbptr.clear();
    top_limbptr2.clear();
    CudaCheckErrorMod;
#ifdef NCCL
    NCCLCHECK(ncclGroupStart());
    for (auto rank : precom.dev_to_communicator) {
        if (rank.second) {
            cudaSetDevice(rank.first);
            NCCLCHECK(ncclCommFinalize(*rank.second));
            // CudaCheckErrorMod;
            NCCLCHECK(ncclCommDestroy(*rank.second));
            // CudaCheckErrorMod;
        }
    }
    NCCLCHECK(ncclGroupEnd());
#endif
    Ciphertext::clearOpRecord();
    CudaCheckErrorMod;
}

bool ContextData::hasAuxilarPoly() const {
    return precom.auxPoly.empty();
}

RNSPoly ContextData::getAuxilarPoly() {

    if (precom.auxPoly.empty()) {
        return RNSPoly(*this);
    } else {

----- /root/fideslib/src/CKKS/Context.cu:889 -----
            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
#endif
        } else {
            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
        }
        top_limbptr[i].free(top_limb_stream[g]);
        top_limbptr2[i].free(top_limb_stream2[g]);
    }
    top_limbptr.clear();
    top_limbptr2.clear();
    CudaCheckErrorMod;
#ifdef NCCL
    NCCLCHECK(ncclGroupStart());
    for (auto rank : precom.dev_to_communicator) {
        if (rank.second) {
            cudaSetDevice(rank.first);
            NCCLCHECK(ncclCommFinalize(*rank.second));
            // CudaCheckErrorMod;
            NCCLCHECK(ncclCommDestroy(*rank.second));
            // CudaCheckErrorMod;
        }
    }
    NCCLCHECK(ncclGroupEnd());
#endif
    Ciphertext::clearOpRecord();
    CudaCheckErrorMod;
}

bool ContextData::hasAuxilarPoly() const {
    return precom.auxPoly.empty();
}

RNSPoly ContextData::getAuxilarPoly() {

    if (precom.auxPoly.empty()) {
        return RNSPoly(*this);
    } else {
        RNSPoly res(std::move(precom.auxPoly.back()));
        precom.auxPoly.pop_back();

----- /root/fideslib/src/CKKS/Context.cu:893 -----
            cudaFree(top_limb_buffer[i]);
            cudaFree(top_limb_buffer2[i]);
        }
        top_limbptr[i].free(top_limb_stream[g]);
        top_limbptr2[i].free(top_limb_stream2[g]);
    }
    top_limbptr.clear();
    top_limbptr2.clear();
    CudaCheckErrorMod;
#ifdef NCCL
    NCCLCHECK(ncclGroupStart());
    for (auto rank : precom.dev_to_communicator) {
        if (rank.second) {
            cudaSetDevice(rank.first);
            NCCLCHECK(ncclCommFinalize(*rank.second));
            // CudaCheckErrorMod;
            NCCLCHECK(ncclCommDestroy(*rank.second));
            // CudaCheckErrorMod;
        }
    }
    NCCLCHECK(ncclGroupEnd());
#endif
    Ciphertext::clearOpRecord();
    CudaCheckErrorMod;
}

bool ContextData::hasAuxilarPoly() const {
    return precom.auxPoly.empty();
}

RNSPoly ContextData::getAuxilarPoly() {

    if (precom.auxPoly.empty()) {
        return RNSPoly(*this);
    } else {
        RNSPoly res(std::move(precom.auxPoly.back()));
        precom.auxPoly.pop_back();
        return res;
    }
}


---------------- FILE: /root/fideslib/src/CKKS/Context.cuh ----------------

----- /root/fideslib/src/CKKS/Context.cuh:19 -----
//
// Created by carlos on 6/03/24.
//

#ifndef FIDESLIB_CKKS_CONTEXT_CUH
#define FIDESLIB_CKKS_CONTEXT_CUH

#include "ConstantsGPU.cuh"
#include "LimbUtils.cuh"
#include "Parameters.cuh"
#include "RNSPoly.cuh"

#include <array>
#include <cassert>
#include <iostream>
#include <list>

#ifdef NCCL
#include "nccl.h"
#endif

namespace FIDESlib::CKKS {

struct Precomputations {
    std::vector<Constants> constants;
    std::unique_ptr<Global> globals;
    std::map<int, BootstrapPrecomputation> boot;
    std::vector<RNSPoly> auxPoly;
    std::map<int, RNSPoly> monomialCache;
#ifdef NCCL
    std::map<int, ncclComm_t*> dev_to_communicator;
#endif
    struct KeyPrecomputations {
        std::unique_ptr<KeySwitchingKey> eval_key;
        std::map<int, KeySwitchingKey> rot_keys;
    };
    std::map<KeyHash, KeyPrecomputations> keys;
};


----- /root/fideslib/src/CKKS/Context.cuh:31 -----
#include "RNSPoly.cuh"

#include <array>
#include <cassert>
#include <iostream>
#include <list>

#ifdef NCCL
#include "nccl.h"
#endif

namespace FIDESlib::CKKS {

struct Precomputations {
    std::vector<Constants> constants;
    std::unique_ptr<Global> globals;
    std::map<int, BootstrapPrecomputation> boot;
    std::vector<RNSPoly> auxPoly;
    std::map<int, RNSPoly> monomialCache;
#ifdef NCCL
    std::map<int, ncclComm_t*> dev_to_communicator;
#endif
    struct KeyPrecomputations {
        std::unique_ptr<KeySwitchingKey> eval_key;
        std::map<int, KeySwitchingKey> rot_keys;
    };
    std::map<KeyHash, KeyPrecomputations> keys;
};

enum RESCALE_TECHNIQUE { NO_RESCALE, FIXEDMANUAL, FIXEDAUTO, FLEXIBLEAUTO, FLEXIBLEAUTOEXT };

extern std::atomic_uint64_t next_uid;

class ContextData {
   public:
    static constexpr const char* loc{"Context"};
    CudaNvtxRange my_range;
    Parameters param;
    Precomputations precom;
    const int logN;
    const int N;

----- /root/fideslib/src/CKKS/Context.cuh:76 -----
    const std::vector<int> GPUid;
    const int& dnum;
    std::vector<std::vector<int>> GPUdigits;
    const std::vector<PrimeRecord>& prime;
    std::vector<std::vector<LimbRecord>> meta;
    const std::vector<int> logQ_d;
    const int& K;
    const int logP;
    const std::vector<PrimeRecord>& specialPrime;

    std::vector<std::vector<LimbRecord>> specialMeta;  // Make const maybe
    std::vector<std::vector<LimbRecord>> splitSpecialMeta;
    std::vector<std::vector<std::vector<LimbRecord>>> decompMeta;  // Make const maybe
    std::vector<std::vector<std::vector<LimbRecord>>> digitMeta;   // Make const maybe
    std::vector<LimbRecord> gatherMeta;

    const std::vector<dim3> limbGPUid;
    const std::vector<int> digitGPUid;

#ifdef NCCL
    ncclUniqueId communicatorID;
    std::vector<ncclComm_t> GPUrank;
#else
    std::vector<int> GPUrank;
#endif

    std::unique_ptr<RNSPoly> key_switch_aux = nullptr;
    std::unique_ptr<RNSPoly> key_switch_aux2 = nullptr;
    std::array<std::unique_ptr<RNSPoly>, 2> moddown_aux = {nullptr};
    std::vector<Stream> top_limb_stream;
    std::vector<uint64_t*> top_limb_buffer;
    std::vector<void*> top_limb_buffer_handle;
    std::vector<VectorGPU<void*>> top_limbptr;

    std::vector<Stream> top_limb_stream2;
    std::vector<uint64_t*> top_limb_buffer2;
    std::vector<void*> top_limb_buffer2_handle;
    std::vector<VectorGPU<void*>> top_limbptr2;

    std::vector<std::vector<Stream>> gatherStream;
    std::vector<std::vector<Stream>> digitStream;

----- /root/fideslib/src/CKKS/Context.cuh:77 -----
    const int& dnum;
    std::vector<std::vector<int>> GPUdigits;
    const std::vector<PrimeRecord>& prime;
    std::vector<std::vector<LimbRecord>> meta;
    const std::vector<int> logQ_d;
    const int& K;
    const int logP;
    const std::vector<PrimeRecord>& specialPrime;

    std::vector<std::vector<LimbRecord>> specialMeta;  // Make const maybe
    std::vector<std::vector<LimbRecord>> splitSpecialMeta;
    std::vector<std::vector<std::vector<LimbRecord>>> decompMeta;  // Make const maybe
    std::vector<std::vector<std::vector<LimbRecord>>> digitMeta;   // Make const maybe
    std::vector<LimbRecord> gatherMeta;

    const std::vector<dim3> limbGPUid;
    const std::vector<int> digitGPUid;

#ifdef NCCL
    ncclUniqueId communicatorID;
    std::vector<ncclComm_t> GPUrank;
#else
    std::vector<int> GPUrank;
#endif

    std::unique_ptr<RNSPoly> key_switch_aux = nullptr;
    std::unique_ptr<RNSPoly> key_switch_aux2 = nullptr;
    std::array<std::unique_ptr<RNSPoly>, 2> moddown_aux = {nullptr};
    std::vector<Stream> top_limb_stream;
    std::vector<uint64_t*> top_limb_buffer;
    std::vector<void*> top_limb_buffer_handle;
    std::vector<VectorGPU<void*>> top_limbptr;

    std::vector<Stream> top_limb_stream2;
    std::vector<uint64_t*> top_limb_buffer2;
    std::vector<void*> top_limb_buffer2_handle;
    std::vector<VectorGPU<void*>> top_limbptr2;

    std::vector<std::vector<Stream>> gatherStream;
    std::vector<std::vector<Stream>> digitStream;
    std::vector<std::vector<std::vector<Stream>>> digitStreamForMemcpyPeer;

---------------- FILE: /root/fideslib/src/CKKS/LimbPartitionMGPU.cu ----------------

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:135 -----
                      << limb.size() << std::endl;
        }
    } else {
        //stream.wait(s);
    }

    if (cc.GPUid.size() > 1) {
        if (MEMCPY_PEER) {

            int top_gpu = cc.limbGPUid[*level].x;
            if (top_gpu == id)
                stream.record();

            openmp_synchronize();

            if (top_gpu != id) {
                for (int i = 0; i < cc.GPUid.size(); ++i) {
                    if (i == id) {
                        //cudaSetDevice(cc.GPUid[i]);
                        stream.wait(cc.top_limb_stream[top_gpu]);
                        cudaMemcpyPeerAsync(buffer, cc.GPUid[i], cc.top_limb_buffer[top_gpu], cc.GPUid[top_gpu],
                                            cc.N * sizeof(uint64_t), stream.ptr());
                    }
                    CudaCheckErrorModNoSync;
                }
                cudaSetDevice(device);
            }
        } else {

#ifdef NCCL
            if constexpr (0) {
                NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr()));
            } else {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id)
                            ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
                    }
                } else {
                    ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr());

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:146 -----
                stream.record();

            openmp_synchronize();

            if (top_gpu != id) {
                for (int i = 0; i < cc.GPUid.size(); ++i) {
                    if (i == id) {
                        //cudaSetDevice(cc.GPUid[i]);
                        stream.wait(cc.top_limb_stream[top_gpu]);
                        cudaMemcpyPeerAsync(buffer, cc.GPUid[i], cc.top_limb_buffer[top_gpu], cc.GPUid[top_gpu],
                                            cc.N * sizeof(uint64_t), stream.ptr());
                    }
                    CudaCheckErrorModNoSync;
                }
                cudaSetDevice(device);
            }
        } else {

#ifdef NCCL
            if constexpr (0) {
                NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr()));
            } else {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id)
                            ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
                    }
                } else {
                    ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
    }
    /* {
        std::vector<uint64_t> data(cc.N, 0);
        cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:148 -----
            openmp_synchronize();

            if (top_gpu != id) {
                for (int i = 0; i < cc.GPUid.size(); ++i) {
                    if (i == id) {
                        //cudaSetDevice(cc.GPUid[i]);
                        stream.wait(cc.top_limb_stream[top_gpu]);
                        cudaMemcpyPeerAsync(buffer, cc.GPUid[i], cc.top_limb_buffer[top_gpu], cc.GPUid[top_gpu],
                                            cc.N * sizeof(uint64_t), stream.ptr());
                    }
                    CudaCheckErrorModNoSync;
                }
                cudaSetDevice(device);
            }
        } else {

#ifdef NCCL
            if constexpr (0) {
                NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr()));
            } else {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id)
                            ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
                    }
                } else {
                    ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
    }
    /* {
        std::vector<uint64_t> data(cc.N, 0);
        cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());
        cudaDeviceSynchronize();
        std::cout << "GPU " << id << " (" << data[0] << " " << data[1] << ")" << std::endl;

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:152 -----
                    if (i == id) {
                        //cudaSetDevice(cc.GPUid[i]);
                        stream.wait(cc.top_limb_stream[top_gpu]);
                        cudaMemcpyPeerAsync(buffer, cc.GPUid[i], cc.top_limb_buffer[top_gpu], cc.GPUid[top_gpu],
                                            cc.N * sizeof(uint64_t), stream.ptr());
                    }
                    CudaCheckErrorModNoSync;
                }
                cudaSetDevice(device);
            }
        } else {

#ifdef NCCL
            if constexpr (0) {
                NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr()));
            } else {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id)
                            ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
                    }
                } else {
                    ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
    }
    /* {
        std::vector<uint64_t> data(cc.N, 0);
        cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());
        cudaDeviceSynchronize();
        std::cout << "GPU " << id << " (" << data[0] << " " << data[1] << ")" << std::endl;
    } */

    {
        {

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:155 -----
                        cudaMemcpyPeerAsync(buffer, cc.GPUid[i], cc.top_limb_buffer[top_gpu], cc.GPUid[top_gpu],
                                            cc.N * sizeof(uint64_t), stream.ptr());
                    }
                    CudaCheckErrorModNoSync;
                }
                cudaSetDevice(device);
            }
        } else {

#ifdef NCCL
            if constexpr (0) {
                NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr()));
            } else {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id)
                            ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
                    }
                } else {
                    ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
    }
    /* {
        std::vector<uint64_t> data(cc.N, 0);
        cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());
        cudaDeviceSynchronize();
        std::cout << "GPU " << id << " (" << data[0] << " " << data[1] << ")" << std::endl;
    } */

    {
        {
            stream.wait(s);
            constexpr ALGO algo = ALGO_SHOUP;
            constexpr int M = 4;

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:157 -----
                    }
                    CudaCheckErrorModNoSync;
                }
                cudaSetDevice(device);
            }
        } else {

#ifdef NCCL
            if constexpr (0) {
                NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr()));
            } else {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id)
                            ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
                    }
                } else {
                    ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[*level].x, rank, stream.ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
    }
    /* {
        std::vector<uint64_t> data(cc.N, 0);
        cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());
        cudaDeviceSynchronize();
        std::cout << "GPU " << id << " (" << data[0] << " " << data[1] << ")" << std::endl;
    } */

    {
        {
            stream.wait(s);
            constexpr ALGO algo = ALGO_SHOUP;
            constexpr int M = 4;

            const dim3 blockDimFirst{(uint32_t)(1 << ((cc.logN + 1) / 2 - 1))};

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:309 -----
            if (top_gpu == id) {
                stream[0]->record();
                stream[1]->record();
            }
            openmp_synchronize();

            if (top_gpu != id) {
                for (int i = 0; i < cc.GPUid.size(); ++i) {

                    if (i == id) {
                        {
                            // auto& stream_0 = cc.digitStreamForMemcpyPeer[0].at(id).at(i);
                            // auto& stream_1 = cc.digitStreamForMemcpyPeer[cc.dnum > 1 ? 1 : 0].at(id).at(i);

                            if (limbsize > 0) {
                                stream[0]->wait(cc.top_limb_stream[top_gpu]);
                                stream[1]->wait(cc.top_limb_stream2[top_gpu]);

                                //stream_0.wait(cc.top_limb_stream[i]);
                                //stream_1.wait(cc.top_limb_stream2[i]);
                                cudaMemcpyPeerAsync(buffer[0], cc.GPUid[i], cc.top_limb_buffer[top_gpu],
                                                    cc.GPUid[top_gpu], cc.N * sizeof(uint64_t), stream[0]->ptr());
                                cudaMemcpyPeerAsync(buffer[1], cc.GPUid[i], cc.top_limb_buffer2[top_gpu],
                                                    cc.GPUid[top_gpu], cc.N * sizeof(uint64_t), stream[1]->ptr());
                            }
                        }
                    }
                    CudaCheckErrorModNoSync;
                }

                cudaSetDevice(device);
            }
        } else {
#ifdef NCCL
            {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id) {
                            ncclSend(buffer[0], cc.N, ncclUint64, i, rank, stream[0]->ptr());
                            ncclSend(buffer[1], cc.N, ncclUint64, i, rank, stream[1]->ptr());

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:311 -----
                stream[1]->record();
            }
            openmp_synchronize();

            if (top_gpu != id) {
                for (int i = 0; i < cc.GPUid.size(); ++i) {

                    if (i == id) {
                        {
                            // auto& stream_0 = cc.digitStreamForMemcpyPeer[0].at(id).at(i);
                            // auto& stream_1 = cc.digitStreamForMemcpyPeer[cc.dnum > 1 ? 1 : 0].at(id).at(i);

                            if (limbsize > 0) {
                                stream[0]->wait(cc.top_limb_stream[top_gpu]);
                                stream[1]->wait(cc.top_limb_stream2[top_gpu]);

                                //stream_0.wait(cc.top_limb_stream[i]);
                                //stream_1.wait(cc.top_limb_stream2[i]);
                                cudaMemcpyPeerAsync(buffer[0], cc.GPUid[i], cc.top_limb_buffer[top_gpu],
                                                    cc.GPUid[top_gpu], cc.N * sizeof(uint64_t), stream[0]->ptr());
                                cudaMemcpyPeerAsync(buffer[1], cc.GPUid[i], cc.top_limb_buffer2[top_gpu],
                                                    cc.GPUid[top_gpu], cc.N * sizeof(uint64_t), stream[1]->ptr());
                            }
                        }
                    }
                    CudaCheckErrorModNoSync;
                }

                cudaSetDevice(device);
            }
        } else {
#ifdef NCCL
            {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id) {
                            ncclSend(buffer[0], cc.N, ncclUint64, i, rank, stream[0]->ptr());
                            ncclSend(buffer[1], cc.N, ncclUint64, i, rank, stream[1]->ptr());
                        }
                    }

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:324 -----
                                stream[0]->wait(cc.top_limb_stream[top_gpu]);
                                stream[1]->wait(cc.top_limb_stream2[top_gpu]);

                                //stream_0.wait(cc.top_limb_stream[i]);
                                //stream_1.wait(cc.top_limb_stream2[i]);
                                cudaMemcpyPeerAsync(buffer[0], cc.GPUid[i], cc.top_limb_buffer[top_gpu],
                                                    cc.GPUid[top_gpu], cc.N * sizeof(uint64_t), stream[0]->ptr());
                                cudaMemcpyPeerAsync(buffer[1], cc.GPUid[i], cc.top_limb_buffer2[top_gpu],
                                                    cc.GPUid[top_gpu], cc.N * sizeof(uint64_t), stream[1]->ptr());
                            }
                        }
                    }
                    CudaCheckErrorModNoSync;
                }

                cudaSetDevice(device);
            }
        } else {
#ifdef NCCL
            {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id) {
                            ncclSend(buffer[0], cc.N, ncclUint64, i, rank, stream[0]->ptr());
                            ncclSend(buffer[1], cc.N, ncclUint64, i, rank, stream[1]->ptr());
                        }
                    }
                } else {
                    ncclRecv(buffer[0], cc.N, ncclUint64, cc.limbGPUid[*part[0]->level].x, rank, stream[0]->ptr());
                    ncclRecv(buffer[1], cc.N, ncclUint64, cc.limbGPUid[*part[1]->level].x, rank, stream[1]->ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
        /* {
            std::vector<uint64_t> data(cc.N, 0);
            cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:328 -----
                                //stream_1.wait(cc.top_limb_stream2[i]);
                                cudaMemcpyPeerAsync(buffer[0], cc.GPUid[i], cc.top_limb_buffer[top_gpu],
                                                    cc.GPUid[top_gpu], cc.N * sizeof(uint64_t), stream[0]->ptr());
                                cudaMemcpyPeerAsync(buffer[1], cc.GPUid[i], cc.top_limb_buffer2[top_gpu],
                                                    cc.GPUid[top_gpu], cc.N * sizeof(uint64_t), stream[1]->ptr());
                            }
                        }
                    }
                    CudaCheckErrorModNoSync;
                }

                cudaSetDevice(device);
            }
        } else {
#ifdef NCCL
            {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id) {
                            ncclSend(buffer[0], cc.N, ncclUint64, i, rank, stream[0]->ptr());
                            ncclSend(buffer[1], cc.N, ncclUint64, i, rank, stream[1]->ptr());
                        }
                    }
                } else {
                    ncclRecv(buffer[0], cc.N, ncclUint64, cc.limbGPUid[*part[0]->level].x, rank, stream[0]->ptr());
                    ncclRecv(buffer[1], cc.N, ncclUint64, cc.limbGPUid[*part[1]->level].x, rank, stream[1]->ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
        /* {
            std::vector<uint64_t> data(cc.N, 0);
            cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());
            cudaDeviceSynchronize();
            std::cout << "GPU " << id << " (" << data[0] << " " << data[1] << ")" << std::endl;
        } */
    }

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:329 -----
                                cudaMemcpyPeerAsync(buffer[0], cc.GPUid[i], cc.top_limb_buffer[top_gpu],
                                                    cc.GPUid[top_gpu], cc.N * sizeof(uint64_t), stream[0]->ptr());
                                cudaMemcpyPeerAsync(buffer[1], cc.GPUid[i], cc.top_limb_buffer2[top_gpu],
                                                    cc.GPUid[top_gpu], cc.N * sizeof(uint64_t), stream[1]->ptr());
                            }
                        }
                    }
                    CudaCheckErrorModNoSync;
                }

                cudaSetDevice(device);
            }
        } else {
#ifdef NCCL
            {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id) {
                            ncclSend(buffer[0], cc.N, ncclUint64, i, rank, stream[0]->ptr());
                            ncclSend(buffer[1], cc.N, ncclUint64, i, rank, stream[1]->ptr());
                        }
                    }
                } else {
                    ncclRecv(buffer[0], cc.N, ncclUint64, cc.limbGPUid[*part[0]->level].x, rank, stream[0]->ptr());
                    ncclRecv(buffer[1], cc.N, ncclUint64, cc.limbGPUid[*part[1]->level].x, rank, stream[1]->ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
        /* {
            std::vector<uint64_t> data(cc.N, 0);
            cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());
            cudaDeviceSynchronize();
            std::cout << "GPU " << id << " (" << data[0] << " " << data[1] << ")" << std::endl;
        } */
    }
    for (int j = 0; j < 2; ++j) {

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:333 -----
                            }
                        }
                    }
                    CudaCheckErrorModNoSync;
                }

                cudaSetDevice(device);
            }
        } else {
#ifdef NCCL
            {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id) {
                            ncclSend(buffer[0], cc.N, ncclUint64, i, rank, stream[0]->ptr());
                            ncclSend(buffer[1], cc.N, ncclUint64, i, rank, stream[1]->ptr());
                        }
                    }
                } else {
                    ncclRecv(buffer[0], cc.N, ncclUint64, cc.limbGPUid[*part[0]->level].x, rank, stream[0]->ptr());
                    ncclRecv(buffer[1], cc.N, ncclUint64, cc.limbGPUid[*part[1]->level].x, rank, stream[1]->ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
        /* {
            std::vector<uint64_t> data(cc.N, 0);
            cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());
            cudaDeviceSynchronize();
            std::cout << "GPU " << id << " (" << data[0] << " " << data[1] << ")" << std::endl;
        } */
    }
    for (int j = 0; j < 2; ++j) {
        stream[j]->wait(part[j]->s);
        {
            constexpr ALGO algo = ALGO_SHOUP;
            constexpr int M = 4;

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:334 -----
                        }
                    }
                    CudaCheckErrorModNoSync;
                }

                cudaSetDevice(device);
            }
        } else {
#ifdef NCCL
            {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id) {
                            ncclSend(buffer[0], cc.N, ncclUint64, i, rank, stream[0]->ptr());
                            ncclSend(buffer[1], cc.N, ncclUint64, i, rank, stream[1]->ptr());
                        }
                    }
                } else {
                    ncclRecv(buffer[0], cc.N, ncclUint64, cc.limbGPUid[*part[0]->level].x, rank, stream[0]->ptr());
                    ncclRecv(buffer[1], cc.N, ncclUint64, cc.limbGPUid[*part[1]->level].x, rank, stream[1]->ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
        /* {
            std::vector<uint64_t> data(cc.N, 0);
            cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());
            cudaDeviceSynchronize();
            std::cout << "GPU " << id << " (" << data[0] << " " << data[1] << ")" << std::endl;
        } */
    }
    for (int j = 0; j < 2; ++j) {
        stream[j]->wait(part[j]->s);
        {
            constexpr ALGO algo = ALGO_SHOUP;
            constexpr int M = 4;


----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:336 -----
                    CudaCheckErrorModNoSync;
                }

                cudaSetDevice(device);
            }
        } else {
#ifdef NCCL
            {
                NCCLCHECK(ncclGroupStart());
                if (id == cc.limbGPUid[*level].x) {
                    for (int i = 0; i < cc.GPUid.size(); ++i) {
                        if (i != id) {
                            ncclSend(buffer[0], cc.N, ncclUint64, i, rank, stream[0]->ptr());
                            ncclSend(buffer[1], cc.N, ncclUint64, i, rank, stream[1]->ptr());
                        }
                    }
                } else {
                    ncclRecv(buffer[0], cc.N, ncclUint64, cc.limbGPUid[*part[0]->level].x, rank, stream[0]->ptr());
                    ncclRecv(buffer[1], cc.N, ncclUint64, cc.limbGPUid[*part[1]->level].x, rank, stream[1]->ptr());
                }
                NCCLCHECK(ncclGroupEnd());
            }
#else
            assert(false);
#endif
        }
        /* {
            std::vector<uint64_t> data(cc.N, 0);
            cudaMemcpyAsync(data.data(), buffer, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToHost, stream.ptr());
            cudaDeviceSynchronize();
            std::cout << "GPU " << id << " (" << data[0] << " " << data[1] << ")" << std::endl;
        } */
    }
    for (int j = 0; j < 2; ++j) {
        stream[j]->wait(part[j]->s);
        {
            constexpr ALGO algo = ALGO_SHOUP;
            constexpr int M = 4;

            const dim3 blockDimFirst{(uint32_t)(1 << ((cc.logN + 1) / 2 - 1))};
            const dim3 blockDimSecond = dim3{(uint32_t)(1 << ((cc.logN) / 2 - 1))};

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:959 -----

                                        if (j != i) {
                                            Stream& stream_ = cc.digitStreamForMemcpyPeer.at(d).at(i).at(j);
                                            stream_.wait(stream);
                                            if (0 && GRAPH_CAPTURE) {
                                                CudaCheckErrorModNoSync;
                                                transferKernel((float*)(bufferGATHER + cc.N * (start + start_d_i)),
                                                               (float*)(bufferGather_[j] + cc.N * (start + start_d_i)),
                                                               sizeof(uint64_t) * size_d_i * cc.N / sizeof(float),
                                                               stream_.ptr());
                                                CudaCheckErrorModNoSync;
                                                notifyKernel(signal[d][j][i].second, signal[d][j][i].first + 2,
                                                             stream_.ptr());
                                                CudaCheckErrorModNoSync;
                                                join_at_end.insert(&stream_);
                                            } else {
                                                transferKernel((float*)(bufferGATHER + cc.N * (start + start_d_i)),
                                                               (float*)(bufferGather_[j] + cc.N * (start + start_d_i)),
                                                               sizeof(uint64_t) * size_d_i * cc.N / sizeof(float),
                                                               stream_.ptr());
                                                // cudaMemcpyPeerAsync(
                                                //     bufferGather_[j] + cc.N * (start + start_d_i), cc.GPUid[j],
                                                //     bufferGATHER + cc.N * (start + start_d_i), cc.GPUid[i],
                                                //     sizeof(uint64_t) * size_d_i * cc.N, stream_.ptr());
                                                stream_.record();
                                            }
                                        }
                                    }
                                }
                            }
                            start += cc.meta[i].size();
                        }
                    }
                    if (MEMCPY_PEER && GRAPH_CAPTURE) {
                        if (SERIAL_MGPU_PRINT)
                            std::cout << "Thread " << id << " checkpoint " << *thread_stop[id] << std::endl;
                        *thread_stop[id] += 1;
                        if (id != 0) {
                            while (*thread_stop[id] >= *thread_stop[id - 1])
                                ;
                            if (SERIAL_MGPU_PRINT)

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1031 -----
                                            if (0 && GRAPH_CAPTURE) {
                                                CudaCheckErrorModNoSync;
                                                pollingKernel(signal[d][j][i].second, signal[d][j][i].first + 2,
                                                              stream.ptr());
                                                CudaCheckErrorModNoSync;
                                                //signal[d][j][i].first++;
                                                CudaCheckErrorModNoSync;
                                            } else {
                                                Stream& stream_ = cc.digitStreamForMemcpyPeer.at(d).at(i).at(j);
                                                stream.wait(stream_);
                                            }
                                        }
                                    }
                                }
                            }
                            start += cc.meta[i].size();
                        }
                    }
                } else {
#ifdef NCCL
                    NCCLCHECK(ncclGroupStart());

                    int start = 0;
                    for (size_t i = 0; i < cc.GPUid.size(); ++i) {
                        uint32_t limb_size_i = 0;
                        while (limb_size_i < cc.meta[i].size() && cc.meta[i][limb_size_i].id <= *level)
                            limb_size_i++;
                        uint32_t start_d_i = 0;
                        while (start_d_i < limb_size_i && cc.meta[i][start_d_i].digit < d)
                            start_d_i++;
                        uint32_t size_d_i = 0;
                        while (start_d_i + size_d_i < limb_size_i && cc.meta[i][start_d_i + size_d_i].digit < d + ds)
                            size_d_i++;

                        if constexpr (PRINT)
                            if (SELECT) {
                                std::cout << "GPU " << i << " for digits " << d << ":" << d + digits_per_it
                                          << " communicate " << size_d_i << " limbs" << std::endl;
                            }

                        if (size_d_i > 0) {

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1052 -----

                    int start = 0;
                    for (size_t i = 0; i < cc.GPUid.size(); ++i) {
                        uint32_t limb_size_i = 0;
                        while (limb_size_i < cc.meta[i].size() && cc.meta[i][limb_size_i].id <= *level)
                            limb_size_i++;
                        uint32_t start_d_i = 0;
                        while (start_d_i < limb_size_i && cc.meta[i][start_d_i].digit < d)
                            start_d_i++;
                        uint32_t size_d_i = 0;
                        while (start_d_i + size_d_i < limb_size_i && cc.meta[i][start_d_i + size_d_i].digit < d + ds)
                            size_d_i++;

                        if constexpr (PRINT)
                            if (SELECT) {
                                std::cout << "GPU " << i << " for digits " << d << ":" << d + digits_per_it
                                          << " communicate " << size_d_i << " limbs" << std::endl;
                            }

                        if (size_d_i > 0) {
                            NCCLCHECK(ncclBroadcast(
                                /*bufferLIMB + cc.N * start_d*/ bufferGATHER + cc.N * (start + start_d_i),
                                bufferGATHER + cc.N * (start + start_d_i), size_d_i * cc.N, ncclUint64, i, rank,
                                stream.ptr()));
                        }
                        start += cc.meta[i].size();
                    }
                    NCCLCHECK(ncclGroupEnd());
#else
                    assert(false);
#endif
                }
            }
        }
    }

    CudaCheckErrorModNoSync;
    if constexpr (PRINT) {
        if (SELECT) {
            cudaDeviceSynchronize();
            std::cout << "GPU: " << id << "Out INTT after communicate: ";

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1054 -----
                    for (size_t i = 0; i < cc.GPUid.size(); ++i) {
                        uint32_t limb_size_i = 0;
                        while (limb_size_i < cc.meta[i].size() && cc.meta[i][limb_size_i].id <= *level)
                            limb_size_i++;
                        uint32_t start_d_i = 0;
                        while (start_d_i < limb_size_i && cc.meta[i][start_d_i].digit < d)
                            start_d_i++;
                        uint32_t size_d_i = 0;
                        while (start_d_i + size_d_i < limb_size_i && cc.meta[i][start_d_i + size_d_i].digit < d + ds)
                            size_d_i++;

                        if constexpr (PRINT)
                            if (SELECT) {
                                std::cout << "GPU " << i << " for digits " << d << ":" << d + digits_per_it
                                          << " communicate " << size_d_i << " limbs" << std::endl;
                            }

                        if (size_d_i > 0) {
                            NCCLCHECK(ncclBroadcast(
                                /*bufferLIMB + cc.N * start_d*/ bufferGATHER + cc.N * (start + start_d_i),
                                bufferGATHER + cc.N * (start + start_d_i), size_d_i * cc.N, ncclUint64, i, rank,
                                stream.ptr()));
                        }
                        start += cc.meta[i].size();
                    }
                    NCCLCHECK(ncclGroupEnd());
#else
                    assert(false);
#endif
                }
            }
        }
    }

    CudaCheckErrorModNoSync;
    if constexpr (PRINT) {
        if (SELECT) {
            cudaDeviceSynchronize();
            std::cout << "GPU: " << id << "Out INTT after communicate: ";
            for (size_t j = 0; j < DECOMPlimb.size(); ++j) {
                for (size_t i = 0; i < DECOMPlimb[j].size(); ++i) {

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1059 -----
                        while (start_d_i < limb_size_i && cc.meta[i][start_d_i].digit < d)
                            start_d_i++;
                        uint32_t size_d_i = 0;
                        while (start_d_i + size_d_i < limb_size_i && cc.meta[i][start_d_i + size_d_i].digit < d + ds)
                            size_d_i++;

                        if constexpr (PRINT)
                            if (SELECT) {
                                std::cout << "GPU " << i << " for digits " << d << ":" << d + digits_per_it
                                          << " communicate " << size_d_i << " limbs" << std::endl;
                            }

                        if (size_d_i > 0) {
                            NCCLCHECK(ncclBroadcast(
                                /*bufferLIMB + cc.N * start_d*/ bufferGATHER + cc.N * (start + start_d_i),
                                bufferGATHER + cc.N * (start + start_d_i), size_d_i * cc.N, ncclUint64, i, rank,
                                stream.ptr()));
                        }
                        start += cc.meta[i].size();
                    }
                    NCCLCHECK(ncclGroupEnd());
#else
                    assert(false);
#endif
                }
            }
        }
    }

    CudaCheckErrorModNoSync;
    if constexpr (PRINT) {
        if (SELECT) {
            cudaDeviceSynchronize();
            std::cout << "GPU: " << id << "Out INTT after communicate: ";
            for (size_t j = 0; j < DECOMPlimb.size(); ++j) {
                for (size_t i = 0; i < DECOMPlimb[j].size(); ++i) {
                    std::cout << DECOMPmeta[j][i].id;
                    SWITCH(DECOMPlimb[j][i], printThisLimb(2));
                }
                std::cout << std::endl;
            }

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1301 -----
                                        if (0 && GRAPH_CAPTURE) {
                                            CudaCheckErrorModNoSync;
                                            transferKernel(
                                                (float*)(ptr),
                                                (float*)(bufferSpecial_[j] +
                                                         (cc.splitSpecialMeta.at(i).at(0).id - SPECIALmeta.at(0).id) *
                                                             cc.N),
                                                sizeof(uint64_t) * cc.N * num_limbs / sizeof(float), stream_.ptr());
                                            CudaCheckErrorModNoSync;
                                            notifyKernel(signal[cc.dnum + i_][j][i].second,
                                                         signal[cc.dnum + i_][j][i].first + 3, stream_.ptr());
                                            CudaCheckErrorModNoSync;
                                            join_at_end.insert(&stream_);
                                        } else {
                                            transferKernel(
                                                (float*)(ptr),
                                                (float*)(bufferSpecial_[j] +
                                                         (cc.splitSpecialMeta.at(i).at(0).id - SPECIALmeta.at(0).id) *
                                                             cc.N),
                                                sizeof(uint64_t) * cc.N * num_limbs / sizeof(float), stream_.ptr());
                                            //   cudaMemcpyPeerAsync(
                                            //       bufferSpecial_[j] +
                                            //           (cc.splitSpecialMeta.at(i).at(0).id - SPECIALmeta.at(0).id) * cc.N,
                                            //       cc.GPUid[j], ptr, cc.GPUid[i], sizeof(uint64_t) * cc.N * num_limbs,
                                            //       stream_.ptr());
                                            stream_.record();
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
                if (MEMCPY_PEER && GRAPH_CAPTURE) {
                    if (SERIAL_MGPU_PRINT)
                        std::cout << "Thread " << id << " checkpoint " << *thread_stop[id] << std::endl;
                    *thread_stop[id] += 1;
                    if (id != 0) {
                        while (*thread_stop[id] >= *thread_stop[id - 1])
                            ;
                        if (SERIAL_MGPU_PRINT)

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1367 -----
                                        if (0 && GRAPH_CAPTURE) {
                                            CudaCheckErrorModNoSync;
                                            pollingKernel(signal[cc.dnum + i_][j][i].second,
                                                          signal[cc.dnum + i_][j][i].first + 3, stream.ptr());
                                            CudaCheckErrorModNoSync;
                                            //signal[cc.dnum > 1 ? i_ : 0][j][i].first++;
                                        } else {
                                            Stream& stream_ =
                                                cc.digitStreamForMemcpyPeer.at(cc.dnum > 1 ? i_ : 0).at(i).at(j);
                                            stream.wait(stream_);
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            } else {
                for (int i = 0; i < 2; ++i) {
#ifdef NCCL
                    NCCLCHECK(ncclGroupStart());
                    Stream& stream = i == 0 ? s : c0.s;
                    LimbPartition& out = i == 0 ? c1 : c0;
                    uint64_t* bufferSpecial_ = i == 0 ? auxLimbs1.bufferSPECIAL : auxLimbs2.bufferSPECIAL;

                    for (size_t j = 0; j < cc.splitSpecialMeta.size(); ++j) {
                        //Limb<uint64_t>& l = std::get<U64>(
                        //    this->SPECIALlimb.at(cc.splitSpecialMeta.at(i).at(0).id - cc.specialMeta.at(id).at(0).id));
                        const uint32_t num_limbs = cc.splitSpecialMeta.at(j).size();
                        //uint64_t* ptr2 = l.v.data;

                        if (num_limbs > 0) {
                            uint64_t* ptr =
                                bufferSpecial_ + (cc.splitSpecialMeta.at(j).at(0).id - SPECIALmeta.at(0).id) * cc.N;
                            NCCLCHECK(
                                ncclBroadcast(ptr, ptr, cc.N * num_limbs, ncclUint64, (int)j, rank, stream.ptr()));
                        }
                    }
                    NCCLCHECK(ncclGroupEnd());
                }
#else

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1382 -----
                    }
                }
            } else {
                for (int i = 0; i < 2; ++i) {
#ifdef NCCL
                    NCCLCHECK(ncclGroupStart());
                    Stream& stream = i == 0 ? s : c0.s;
                    LimbPartition& out = i == 0 ? c1 : c0;
                    uint64_t* bufferSpecial_ = i == 0 ? auxLimbs1.bufferSPECIAL : auxLimbs2.bufferSPECIAL;

                    for (size_t j = 0; j < cc.splitSpecialMeta.size(); ++j) {
                        //Limb<uint64_t>& l = std::get<U64>(
                        //    this->SPECIALlimb.at(cc.splitSpecialMeta.at(i).at(0).id - cc.specialMeta.at(id).at(0).id));
                        const uint32_t num_limbs = cc.splitSpecialMeta.at(j).size();
                        //uint64_t* ptr2 = l.v.data;

                        if (num_limbs > 0) {
                            uint64_t* ptr =
                                bufferSpecial_ + (cc.splitSpecialMeta.at(j).at(0).id - SPECIALmeta.at(0).id) * cc.N;
                            NCCLCHECK(
                                ncclBroadcast(ptr, ptr, cc.N * num_limbs, ncclUint64, (int)j, rank, stream.ptr()));
                        }
                    }
                    NCCLCHECK(ncclGroupEnd());
                }
#else
                    assert(false);
#endif
            }
        }
    }
    CudaCheckErrorModNoSync;

    if (moddown) {
        if constexpr (PRINT) {
            if (SELECT) {
                cudaDeviceSynchronize();
                std::cout << "GPU: " << id << "KSK specials after INTT and communicate: ";
                for (const auto& j : {&c1, &c0}) {
                    for (auto& i : j->SPECIALlimb) {
                        SWITCH(i, printThisLimb(2));

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1385 -----
                for (int i = 0; i < 2; ++i) {
#ifdef NCCL
                    NCCLCHECK(ncclGroupStart());
                    Stream& stream = i == 0 ? s : c0.s;
                    LimbPartition& out = i == 0 ? c1 : c0;
                    uint64_t* bufferSpecial_ = i == 0 ? auxLimbs1.bufferSPECIAL : auxLimbs2.bufferSPECIAL;

                    for (size_t j = 0; j < cc.splitSpecialMeta.size(); ++j) {
                        //Limb<uint64_t>& l = std::get<U64>(
                        //    this->SPECIALlimb.at(cc.splitSpecialMeta.at(i).at(0).id - cc.specialMeta.at(id).at(0).id));
                        const uint32_t num_limbs = cc.splitSpecialMeta.at(j).size();
                        //uint64_t* ptr2 = l.v.data;

                        if (num_limbs > 0) {
                            uint64_t* ptr =
                                bufferSpecial_ + (cc.splitSpecialMeta.at(j).at(0).id - SPECIALmeta.at(0).id) * cc.N;
                            NCCLCHECK(
                                ncclBroadcast(ptr, ptr, cc.N * num_limbs, ncclUint64, (int)j, rank, stream.ptr()));
                        }
                    }
                    NCCLCHECK(ncclGroupEnd());
                }
#else
                    assert(false);
#endif
            }
        }
    }
    CudaCheckErrorModNoSync;

    if (moddown) {
        if constexpr (PRINT) {
            if (SELECT) {
                cudaDeviceSynchronize();
                std::cout << "GPU: " << id << "KSK specials after INTT and communicate: ";
                for (const auto& j : {&c1, &c0}) {
                    for (auto& i : j->SPECIALlimb) {
                        SWITCH(i, printThisLimb(2));
                    }
                    std::cout << std::endl;
                }

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:1979 -----
                                   cc.meta[i][start_d_i + size_d_i].digit < d + ds)
                                size_d_i++;
                            if constexpr (PRINT)
                                if (SELECT) {
                                    std::cout << "GPU " << i << " for digits " << d << ":" << d + digits_per_it
                                              << " communicate " << size_d_i << " limbs" << std::endl;
                                }
                            if (size_d_i > 0) {

                                if (i == id) {
                                    for (size_t j = 0; j < cc.GPUid.size(); ++j) {

                                        if (j != i) {
                                            Stream& stream_ = cc.digitStreamForMemcpyPeer.at(d).at(i).at(j);
                                            stream_.wait(stream);

                                            transferKernel((float*)(bufferGATHER + cc.N * (start + start_d_i)),
                                                           (float*)(bufferGather_[j] + cc.N * (start + start_d_i)),
                                                           sizeof(uint64_t) * size_d_i * cc.N / sizeof(float),
                                                           stream_.ptr());
                                            //cudaMemcpyPeerAsync(bufferGather_[j] + cc.N * (start + start_d_i),
                                            //                    cc.GPUid[j], bufferGATHER + cc.N * (start + start_d_i),
                                            //                    cc.GPUid[i], sizeof(uint64_t) * size_d_i * cc.N,
                                            //                    stream_.ptr());

                                            //cc.digitStream.at(d).at(j).wait(stream_);
                                            stream_.record();
                                            CudaCheckErrorModNoSync;
                                        }
                                    }

                                    for (size_t j = 0; j < cc.GPUid.size(); ++j) {
                                        if (j != i) {
                                            Stream& stream_ = cc.digitStreamForMemcpyPeer.at(d).at(i).at(j);
                                            //stream.wait(stream_);
                                        }
                                    }
                                }
                            }
                            start += cc.meta[i].size();
                        }

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2044 -----
                            while (start_d_i + size_d_i < limb_size_i &&
                                   cc.meta[i][start_d_i + size_d_i].digit < d + ds)
                                size_d_i++;

                            if (size_d_i > 0) {
                                for (size_t j = 0; j < cc.GPUid.size(); ++j) {
                                    if (j != i) {
                                        if (j == id) {
                                            Stream& stream_ = cc.digitStreamForMemcpyPeer.at(d).at(i).at(j);

                                            stream.wait(stream_);
                                        }
                                    }
                                }
                            }
                            start += cc.meta[i].size();
                        }
                    }
                } else {
#ifdef NCCL
                    NCCLCHECK(ncclGroupStart());
                    int start = 0;
                    for (size_t i = 0; i < cc.GPUid.size(); ++i) {
                        uint32_t limb_size_i = 0;
                        while (limb_size_i < cc.meta[i].size() && cc.meta[i][limb_size_i].id <= *level)
                            limb_size_i++;
                        uint32_t start_d_i = 0;
                        while (start_d_i < limb_size_i && cc.meta[i][start_d_i].digit < d)
                            start_d_i++;
                        uint32_t size_d_i = 0;
                        while (start_d_i + size_d_i < limb_size_i && cc.meta[i][start_d_i + size_d_i].digit < d + ds)
                            size_d_i++;
                        if constexpr (PRINT)
                            if (SELECT) {
                                std::cout << "GPU " << i << " for digits " << d << ":" << d + digits_per_it
                                          << " communicate " << size_d_i << " limbs" << std::endl;
                            }
                        if (size_d_i > 0) {

                            NCCLCHECK(ncclBroadcast(bufferGATHER + cc.N * (start + start_d_i),
                                                    bufferGATHER + cc.N * (start + start_d_i), size_d_i * cc.N,

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2063 -----
#ifdef NCCL
                    NCCLCHECK(ncclGroupStart());
                    int start = 0;
                    for (size_t i = 0; i < cc.GPUid.size(); ++i) {
                        uint32_t limb_size_i = 0;
                        while (limb_size_i < cc.meta[i].size() && cc.meta[i][limb_size_i].id <= *level)
                            limb_size_i++;
                        uint32_t start_d_i = 0;
                        while (start_d_i < limb_size_i && cc.meta[i][start_d_i].digit < d)
                            start_d_i++;
                        uint32_t size_d_i = 0;
                        while (start_d_i + size_d_i < limb_size_i && cc.meta[i][start_d_i + size_d_i].digit < d + ds)
                            size_d_i++;
                        if constexpr (PRINT)
                            if (SELECT) {
                                std::cout << "GPU " << i << " for digits " << d << ":" << d + digits_per_it
                                          << " communicate " << size_d_i << " limbs" << std::endl;
                            }
                        if (size_d_i > 0) {

                            NCCLCHECK(ncclBroadcast(bufferGATHER + cc.N * (start + start_d_i),
                                                    bufferGATHER + cc.N * (start + start_d_i), size_d_i * cc.N,
                                                    ncclUint64, i, rank, stream.ptr()));
                        }
                        start += cc.meta[i].size();
                    }
                    NCCLCHECK(ncclGroupEnd());
#else
                    assert(false);
#endif
                }
            }
        }
    }
    if constexpr (PRINT) {
        if (SELECT) {
            cudaDeviceSynchronize();
            std::cout << "GPU: " << id << "Out INTT after communicate: ";
            for (size_t j = 0; j < DECOMPlimb.size(); ++j) {
                for (size_t i = 0; i < DECOMPlimb[j].size(); ++i) {
                    std::cout << DECOMPmeta[j][i].id;

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2065 -----
                    int start = 0;
                    for (size_t i = 0; i < cc.GPUid.size(); ++i) {
                        uint32_t limb_size_i = 0;
                        while (limb_size_i < cc.meta[i].size() && cc.meta[i][limb_size_i].id <= *level)
                            limb_size_i++;
                        uint32_t start_d_i = 0;
                        while (start_d_i < limb_size_i && cc.meta[i][start_d_i].digit < d)
                            start_d_i++;
                        uint32_t size_d_i = 0;
                        while (start_d_i + size_d_i < limb_size_i && cc.meta[i][start_d_i + size_d_i].digit < d + ds)
                            size_d_i++;
                        if constexpr (PRINT)
                            if (SELECT) {
                                std::cout << "GPU " << i << " for digits " << d << ":" << d + digits_per_it
                                          << " communicate " << size_d_i << " limbs" << std::endl;
                            }
                        if (size_d_i > 0) {

                            NCCLCHECK(ncclBroadcast(bufferGATHER + cc.N * (start + start_d_i),
                                                    bufferGATHER + cc.N * (start + start_d_i), size_d_i * cc.N,
                                                    ncclUint64, i, rank, stream.ptr()));
                        }
                        start += cc.meta[i].size();
                    }
                    NCCLCHECK(ncclGroupEnd());
#else
                    assert(false);
#endif
                }
            }
        }
    }
    if constexpr (PRINT) {
        if (SELECT) {
            cudaDeviceSynchronize();
            std::cout << "GPU: " << id << "Out INTT after communicate: ";
            for (size_t j = 0; j < DECOMPlimb.size(); ++j) {
                for (size_t i = 0; i < DECOMPlimb[j].size(); ++i) {
                    std::cout << DECOMPmeta[j][i].id;
                    SWITCH(DECOMPlimb[j][i], printThisLimb(2));
                }

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2069 -----
                            limb_size_i++;
                        uint32_t start_d_i = 0;
                        while (start_d_i < limb_size_i && cc.meta[i][start_d_i].digit < d)
                            start_d_i++;
                        uint32_t size_d_i = 0;
                        while (start_d_i + size_d_i < limb_size_i && cc.meta[i][start_d_i + size_d_i].digit < d + ds)
                            size_d_i++;
                        if constexpr (PRINT)
                            if (SELECT) {
                                std::cout << "GPU " << i << " for digits " << d << ":" << d + digits_per_it
                                          << " communicate " << size_d_i << " limbs" << std::endl;
                            }
                        if (size_d_i > 0) {

                            NCCLCHECK(ncclBroadcast(bufferGATHER + cc.N * (start + start_d_i),
                                                    bufferGATHER + cc.N * (start + start_d_i), size_d_i * cc.N,
                                                    ncclUint64, i, rank, stream.ptr()));
                        }
                        start += cc.meta[i].size();
                    }
                    NCCLCHECK(ncclGroupEnd());
#else
                    assert(false);
#endif
                }
            }
        }
    }
    if constexpr (PRINT) {
        if (SELECT) {
            cudaDeviceSynchronize();
            std::cout << "GPU: " << id << "Out INTT after communicate: ";
            for (size_t j = 0; j < DECOMPlimb.size(); ++j) {
                for (size_t i = 0; i < DECOMPlimb[j].size(); ++i) {
                    std::cout << DECOMPmeta[j][i].id;
                    SWITCH(DECOMPlimb[j][i], printThisLimb(2));
                }
                std::cout << std::endl;
            }
            cudaDeviceSynchronize();
        }

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2399 -----
                    CudaCheckErrorModNoSync;
                    openmp_synchronize();

                    for (size_t i = 0; i < cc.splitSpecialMeta.size(); ++i) {
                        //Limb<uint64_t>& l = std::get<U64>(
                        //    this->SPECIALlimb.at(cc.splitSpecialMeta.at(i).at(0).id - cc.specialMeta.at(id).at(0).id));
                        const uint32_t num_limbs = cc.splitSpecialMeta.at(i).size();
                        //uint64_t* ptr2 = l.v.data;

                        if (id == i) {
                            if (num_limbs > 0) {
                                uint64_t* ptr = auxLimbs.bufferSPECIAL +
                                                (cc.splitSpecialMeta.at(i).at(0).id - SPECIALmeta.at(0).id) * cc.N;

                                for (int j = 0; j < cc.GPUid.size(); ++j) {
                                    if (j != i) {
                                        Stream& stream_ = cc.digitStreamForMemcpyPeer.at(0).at(i).at(j);
                                        stream_.wait(stream);

                                        CudaCheckErrorModNoSync;
                                        cudaMemcpyPeerAsync(
                                            bufferSpecial_[j] +
                                                (cc.splitSpecialMeta.at(i).at(0).id - SPECIALmeta.at(0).id) * cc.N,
                                            cc.GPUid[j], ptr, cc.GPUid[i], sizeof(uint64_t) * cc.N * num_limbs,
                                            stream_.ptr());
                                        CudaCheckErrorModNoSync;
                                    }
                                }

                                for (int j = 0; j < cc.GPUid.size(); ++j) {
                                    if (j != i) {
                                        Stream& stream_ = cc.digitStreamForMemcpyPeer.at(0).at(id).at(j);
                                        //stream.wait(stream_);
                                    }
                                }
                            }
                        }
                    }
                    openmp_synchronize();

                    for (size_t i = 0; i < cc.splitSpecialMeta.size(); ++i) {

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2440 -----
                        //Limb<uint64_t>& l = std::get<U64>(
                        //    this->SPECIALlimb.at(cc.splitSpecialMeta.at(i).at(0).id - cc.specialMeta.at(id).at(0).id));
                        const uint32_t num_limbs = cc.splitSpecialMeta.at(i).size();
                        //uint64_t* ptr2 = l.v.data;

                        if (num_limbs > 0) {

                            for (int j = 0; j < cc.GPUid.size(); ++j) {
                                if (j == id) {
                                    if (i != j) {
                                        Stream& stream_ = cc.digitStreamForMemcpyPeer.at(0).at(i).at(j);
                                        stream.wait(stream_);
                                        CudaCheckErrorModNoSync;
                                    }
                                }
                            }
                        }
                    }
                } else {
#ifdef NCCL
                    NCCLCHECK(ncclGroupStart());
                    for (size_t i = 0; i < cc.splitSpecialMeta.size(); ++i) {
                        //Limb<uint64_t>& l = std::get<U64>(
                        //    this->SPECIALlimb.at(cc.splitSpecialMeta.at(i).at(0).id - cc.specialMeta.at(id).at(0).id));
                        const uint32_t num_limbs = cc.splitSpecialMeta.at(i).size();
                        //uint64_t* ptr2 = l.v.data;
                        if (num_limbs > 0) {
                            uint64_t* ptr = auxLimbs.bufferSPECIAL +
                                            (cc.splitSpecialMeta.at(i).at(0).id - SPECIALmeta.at(0).id) * cc.N;
                            NCCLCHECK(
                                ncclBroadcast(ptr, ptr, cc.N * num_limbs, ncclUint64, (int)i, rank, stream.ptr()));
                        }
                    }
                    NCCLCHECK(ncclGroupEnd());
#else
                    assert(false);
#endif
                }
            }
        }
    }

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2450 -----
                                        Stream& stream_ = cc.digitStreamForMemcpyPeer.at(0).at(i).at(j);
                                        stream.wait(stream_);
                                        CudaCheckErrorModNoSync;
                                    }
                                }
                            }
                        }
                    }
                } else {
#ifdef NCCL
                    NCCLCHECK(ncclGroupStart());
                    for (size_t i = 0; i < cc.splitSpecialMeta.size(); ++i) {
                        //Limb<uint64_t>& l = std::get<U64>(
                        //    this->SPECIALlimb.at(cc.splitSpecialMeta.at(i).at(0).id - cc.specialMeta.at(id).at(0).id));
                        const uint32_t num_limbs = cc.splitSpecialMeta.at(i).size();
                        //uint64_t* ptr2 = l.v.data;
                        if (num_limbs > 0) {
                            uint64_t* ptr = auxLimbs.bufferSPECIAL +
                                            (cc.splitSpecialMeta.at(i).at(0).id - SPECIALmeta.at(0).id) * cc.N;
                            NCCLCHECK(
                                ncclBroadcast(ptr, ptr, cc.N * num_limbs, ncclUint64, (int)i, rank, stream.ptr()));
                        }
                    }
                    NCCLCHECK(ncclGroupEnd());
#else
                    assert(false);
#endif
                }
            }
        }
    }

    if constexpr (PRINT) {
        if (SELECT) {
            cudaDeviceSynchronize();
            std::cout << "GPU: " << id << "KSK specials after INTT and communicate: ";
            for (const auto& j : {&c1}) {
                for (auto& i : j->SPECIALlimb) {
                    SWITCH(i, printThisLimb(2));
                }
                std::cout << std::endl;

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2453 -----
                                    }
                                }
                            }
                        }
                    }
                } else {
#ifdef NCCL
                    NCCLCHECK(ncclGroupStart());
                    for (size_t i = 0; i < cc.splitSpecialMeta.size(); ++i) {
                        //Limb<uint64_t>& l = std::get<U64>(
                        //    this->SPECIALlimb.at(cc.splitSpecialMeta.at(i).at(0).id - cc.specialMeta.at(id).at(0).id));
                        const uint32_t num_limbs = cc.splitSpecialMeta.at(i).size();
                        //uint64_t* ptr2 = l.v.data;
                        if (num_limbs > 0) {
                            uint64_t* ptr = auxLimbs.bufferSPECIAL +
                                            (cc.splitSpecialMeta.at(i).at(0).id - SPECIALmeta.at(0).id) * cc.N;
                            NCCLCHECK(
                                ncclBroadcast(ptr, ptr, cc.N * num_limbs, ncclUint64, (int)i, rank, stream.ptr()));
                        }
                    }
                    NCCLCHECK(ncclGroupEnd());
#else
                    assert(false);
#endif
                }
            }
        }
    }

    if constexpr (PRINT) {
        if (SELECT) {
            cudaDeviceSynchronize();
            std::cout << "GPU: " << id << "KSK specials after INTT and communicate: ";
            for (const auto& j : {&c1}) {
                for (auto& i : j->SPECIALlimb) {
                    SWITCH(i, printThisLimb(2));
                }
                std::cout << std::endl;
            }
            std::cout << std::endl;
            cudaDeviceSynchronize();

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2588 -----
}

void LimbPartition::broadcastLimb0_mgpu() {
    cudaSetDevice(device);
    static bool parity = true;
    const int limbsize = getLimbSize(*level);

    Stream& stream = parity ? cc.top_limb_stream[id] : cc.top_limb_stream2[id];
    uint64_t* buffer = parity ? cc.top_limb_buffer[id] : cc.top_limb_buffer2[id];
    VectorGPU<void*>& ptr = parity ? cc.top_limbptr[id] : cc.top_limbptr2[id];

    stream.wait(s);
    bool skip0 = meta[0].id == 0;

    if (skip0) {
        uint64_t* src_ptr = std::get<U64>(limb[0]).v.data;
        cudaMemcpyAsync(buffer, src_ptr, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToDevice, stream.ptr());
    }
    /*
#ifdef NCCL
    { NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr())); }
#else
    assert(false);
#endif
*/
#ifdef NCCL
    if constexpr (0) {
        NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr()));
    } else {
        NCCLCHECK(ncclGroupStart());
        if (id == cc.limbGPUid[0].x) {
            for (int i = 0; i < cc.GPUid.size(); ++i) {
                if (i != id)
                    ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
            }
        } else {
            ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr());
        }
        NCCLCHECK(ncclGroupEnd());
    }
#else

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2595 -----
    Stream& stream = parity ? cc.top_limb_stream[id] : cc.top_limb_stream2[id];
    uint64_t* buffer = parity ? cc.top_limb_buffer[id] : cc.top_limb_buffer2[id];
    VectorGPU<void*>& ptr = parity ? cc.top_limbptr[id] : cc.top_limbptr2[id];

    stream.wait(s);
    bool skip0 = meta[0].id == 0;

    if (skip0) {
        uint64_t* src_ptr = std::get<U64>(limb[0]).v.data;
        cudaMemcpyAsync(buffer, src_ptr, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToDevice, stream.ptr());
    }
    /*
#ifdef NCCL
    { NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr())); }
#else
    assert(false);
#endif
*/
#ifdef NCCL
    if constexpr (0) {
        NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr()));
    } else {
        NCCLCHECK(ncclGroupStart());
        if (id == cc.limbGPUid[0].x) {
            for (int i = 0; i < cc.GPUid.size(); ++i) {
                if (i != id)
                    ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
            }
        } else {
            ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr());
        }
        NCCLCHECK(ncclGroupEnd());
    }
#else
    assert(false);
#endif
    if (limbsize - skip0 > 0) {
        CKKS::broadcastLimb0_mgpu<<<dim3{(uint32_t)cc.N / 128, (uint32_t)limbsize - skip0}, 128, 0, stream.ptr()>>>(
            limbptr.data + skip0, PARTITION(id, skip0), ptr.data);
    }
    s.wait(stream);

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2597 -----
    VectorGPU<void*>& ptr = parity ? cc.top_limbptr[id] : cc.top_limbptr2[id];

    stream.wait(s);
    bool skip0 = meta[0].id == 0;

    if (skip0) {
        uint64_t* src_ptr = std::get<U64>(limb[0]).v.data;
        cudaMemcpyAsync(buffer, src_ptr, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToDevice, stream.ptr());
    }
    /*
#ifdef NCCL
    { NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr())); }
#else
    assert(false);
#endif
*/
#ifdef NCCL
    if constexpr (0) {
        NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr()));
    } else {
        NCCLCHECK(ncclGroupStart());
        if (id == cc.limbGPUid[0].x) {
            for (int i = 0; i < cc.GPUid.size(); ++i) {
                if (i != id)
                    ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
            }
        } else {
            ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr());
        }
        NCCLCHECK(ncclGroupEnd());
    }
#else
    assert(false);
#endif
    if (limbsize - skip0 > 0) {
        CKKS::broadcastLimb0_mgpu<<<dim3{(uint32_t)cc.N / 128, (uint32_t)limbsize - skip0}, 128, 0, stream.ptr()>>>(
            limbptr.data + skip0, PARTITION(id, skip0), ptr.data);
    }
    s.wait(stream);
}


----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2601 -----

    if (skip0) {
        uint64_t* src_ptr = std::get<U64>(limb[0]).v.data;
        cudaMemcpyAsync(buffer, src_ptr, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToDevice, stream.ptr());
    }
    /*
#ifdef NCCL
    { NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr())); }
#else
    assert(false);
#endif
*/
#ifdef NCCL
    if constexpr (0) {
        NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr()));
    } else {
        NCCLCHECK(ncclGroupStart());
        if (id == cc.limbGPUid[0].x) {
            for (int i = 0; i < cc.GPUid.size(); ++i) {
                if (i != id)
                    ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
            }
        } else {
            ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr());
        }
        NCCLCHECK(ncclGroupEnd());
    }
#else
    assert(false);
#endif
    if (limbsize - skip0 > 0) {
        CKKS::broadcastLimb0_mgpu<<<dim3{(uint32_t)cc.N / 128, (uint32_t)limbsize - skip0}, 128, 0, stream.ptr()>>>(
            limbptr.data + skip0, PARTITION(id, skip0), ptr.data);
    }
    s.wait(stream);
}

}  // namespace FIDESlib::CKKS

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2604 -----
        cudaMemcpyAsync(buffer, src_ptr, cc.N * sizeof(uint64_t), cudaMemcpyDeviceToDevice, stream.ptr());
    }
    /*
#ifdef NCCL
    { NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr())); }
#else
    assert(false);
#endif
*/
#ifdef NCCL
    if constexpr (0) {
        NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr()));
    } else {
        NCCLCHECK(ncclGroupStart());
        if (id == cc.limbGPUid[0].x) {
            for (int i = 0; i < cc.GPUid.size(); ++i) {
                if (i != id)
                    ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
            }
        } else {
            ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr());
        }
        NCCLCHECK(ncclGroupEnd());
    }
#else
    assert(false);
#endif
    if (limbsize - skip0 > 0) {
        CKKS::broadcastLimb0_mgpu<<<dim3{(uint32_t)cc.N / 128, (uint32_t)limbsize - skip0}, 128, 0, stream.ptr()>>>(
            limbptr.data + skip0, PARTITION(id, skip0), ptr.data);
    }
    s.wait(stream);
}

}  // namespace FIDESlib::CKKS

----- /root/fideslib/src/CKKS/LimbPartitionMGPU.cu:2606 -----
    /*
#ifdef NCCL
    { NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr())); }
#else
    assert(false);
#endif
*/
#ifdef NCCL
    if constexpr (0) {
        NCCLCHECK(ncclBroadcast(buffer, buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr()));
    } else {
        NCCLCHECK(ncclGroupStart());
        if (id == cc.limbGPUid[0].x) {
            for (int i = 0; i < cc.GPUid.size(); ++i) {
                if (i != id)
                    ncclSend(buffer, cc.N, ncclUint64, i, rank, stream.ptr());
            }
        } else {
            ncclRecv(buffer, cc.N, ncclUint64, cc.limbGPUid[0].x, rank, stream.ptr());
        }
        NCCLCHECK(ncclGroupEnd());
    }
#else
    assert(false);
#endif
    if (limbsize - skip0 > 0) {
        CKKS::broadcastLimb0_mgpu<<<dim3{(uint32_t)cc.N / 128, (uint32_t)limbsize - skip0}, 128, 0, stream.ptr()>>>(
            limbptr.data + skip0, PARTITION(id, skip0), ptr.data);
    }
    s.wait(stream);
}

}  // namespace FIDESlib::CKKS

---------------- FILE: /root/fideslib/src/CKKS/LimbPartition.cu ----------------

----- /root/fideslib/src/CKKS/LimbPartition.cu:169 -----

    limbptr.free(s);
    auxptr.free(s);
    SPECIALlimbptr.free(s);
    SPECIALauxptr.free(s);
    GATHERptr.free(s);
    for (auto& d : DECOMPlimbptr)
        d.free(s);
    //    for (auto& d : DECOMPauxptr)
    //        d.free(s);
    for (auto& d : DIGITlimbptr)
        d.free(s);
    // for (auto& d : DIGITauxptr)
    //    d.free(s);

    //CudaCheckErrorMod;
    if (bufferDECOMPandDIGIT_handle) {
        cudaStreamSynchronize(s.ptr());
#ifdef NCCL
        if (bufferDECOMPandDIGIT_handle != (void*)-1)
            NCCLCHECK(ncclCommDeregister(rank, bufferDECOMPandDIGIT_handle));
        NCCLCHECK(ncclMemFree(bufferDECOMPandDIGIT));
#else
        assert(false);
#endif
    } else {
        if (bufferDECOMPandDIGIT)
            GPUfree(bufferDECOMPandDIGIT, id, 0, s.ptr());
        // cudaFreeAsync(bufferDECOMPandDIGIT, s.ptr());
    }
    if (bufferSPECIAL)
        GPUfree(bufferSPECIAL, id, 0, s.ptr());
    //cudaFreeAsync(bufferSPECIAL, s.ptr());
    if (bufferLIMB) {
        GPUfree(bufferLIMB, id, 0, s.ptr());
        //cudaFreeAsync(bufferLIMB, s.ptr());
    }
    if (bufferAUXptrs)
        GPUfree(bufferAUXptrs, id, MAXP * sizeof(void*) * (4ul + 4 * std::max(cc.dnum, 1)), s.ptr(), false);
    // cudaFreeAsync(bufferAUXptrs, s.ptr());
    if (bufferGATHER_handle) {

----- /root/fideslib/src/CKKS/LimbPartition.cu:170 -----
    limbptr.free(s);
    auxptr.free(s);
    SPECIALlimbptr.free(s);
    SPECIALauxptr.free(s);
    GATHERptr.free(s);
    for (auto& d : DECOMPlimbptr)
        d.free(s);
    //    for (auto& d : DECOMPauxptr)
    //        d.free(s);
    for (auto& d : DIGITlimbptr)
        d.free(s);
    // for (auto& d : DIGITauxptr)
    //    d.free(s);

    //CudaCheckErrorMod;
    if (bufferDECOMPandDIGIT_handle) {
        cudaStreamSynchronize(s.ptr());
#ifdef NCCL
        if (bufferDECOMPandDIGIT_handle != (void*)-1)
            NCCLCHECK(ncclCommDeregister(rank, bufferDECOMPandDIGIT_handle));
        NCCLCHECK(ncclMemFree(bufferDECOMPandDIGIT));
#else
        assert(false);
#endif
    } else {
        if (bufferDECOMPandDIGIT)
            GPUfree(bufferDECOMPandDIGIT, id, 0, s.ptr());
        // cudaFreeAsync(bufferDECOMPandDIGIT, s.ptr());
    }
    if (bufferSPECIAL)
        GPUfree(bufferSPECIAL, id, 0, s.ptr());
    //cudaFreeAsync(bufferSPECIAL, s.ptr());
    if (bufferLIMB) {
        GPUfree(bufferLIMB, id, 0, s.ptr());
        //cudaFreeAsync(bufferLIMB, s.ptr());
    }
    if (bufferAUXptrs)
        GPUfree(bufferAUXptrs, id, MAXP * sizeof(void*) * (4ul + 4 * std::max(cc.dnum, 1)), s.ptr(), false);
    // cudaFreeAsync(bufferAUXptrs, s.ptr());
    if (bufferGATHER_handle) {
        cudaStreamSynchronize(s.ptr());

----- /root/fideslib/src/CKKS/LimbPartition.cu:194 -----
    } else {
        if (bufferDECOMPandDIGIT)
            GPUfree(bufferDECOMPandDIGIT, id, 0, s.ptr());
        // cudaFreeAsync(bufferDECOMPandDIGIT, s.ptr());
    }
    if (bufferSPECIAL)
        GPUfree(bufferSPECIAL, id, 0, s.ptr());
    //cudaFreeAsync(bufferSPECIAL, s.ptr());
    if (bufferLIMB) {
        GPUfree(bufferLIMB, id, 0, s.ptr());
        //cudaFreeAsync(bufferLIMB, s.ptr());
    }
    if (bufferAUXptrs)
        GPUfree(bufferAUXptrs, id, MAXP * sizeof(void*) * (4ul + 4 * std::max(cc.dnum, 1)), s.ptr(), false);
    // cudaFreeAsync(bufferAUXptrs, s.ptr());
    if (bufferGATHER_handle) {
        cudaStreamSynchronize(s.ptr());

#ifdef NCCL
        if (bufferGATHER_handle != (void*)-1)
            NCCLCHECK(ncclCommDeregister(rank, bufferGATHER_handle));
        NCCLCHECK(ncclMemFree(bufferGATHER));
#else
        assert(false);
#endif
    } else {
        if (bufferGATHER)
            GPUfree(bufferGATHER, id, 0, s.ptr());
        //cudaFreeAsync(bufferGATHER, s.ptr());
    }
    limb.clear();
    SPECIALlimb.clear();
    DECOMPlimb.clear();
    DIGITlimb.clear();
}

Global::Globals* LimbPartition::getGlobals() {
    return cc.precom.globals->globals[id];
}

void LimbPartition::generate(std::vector<LimbRecord>& records, std::vector<LimbImpl>& limbs, VectorGPU<void*>& ptrs,

----- /root/fideslib/src/CKKS/LimbPartition.cu:195 -----
        if (bufferDECOMPandDIGIT)
            GPUfree(bufferDECOMPandDIGIT, id, 0, s.ptr());
        // cudaFreeAsync(bufferDECOMPandDIGIT, s.ptr());
    }
    if (bufferSPECIAL)
        GPUfree(bufferSPECIAL, id, 0, s.ptr());
    //cudaFreeAsync(bufferSPECIAL, s.ptr());
    if (bufferLIMB) {
        GPUfree(bufferLIMB, id, 0, s.ptr());
        //cudaFreeAsync(bufferLIMB, s.ptr());
    }
    if (bufferAUXptrs)
        GPUfree(bufferAUXptrs, id, MAXP * sizeof(void*) * (4ul + 4 * std::max(cc.dnum, 1)), s.ptr(), false);
    // cudaFreeAsync(bufferAUXptrs, s.ptr());
    if (bufferGATHER_handle) {
        cudaStreamSynchronize(s.ptr());

#ifdef NCCL
        if (bufferGATHER_handle != (void*)-1)
            NCCLCHECK(ncclCommDeregister(rank, bufferGATHER_handle));
        NCCLCHECK(ncclMemFree(bufferGATHER));
#else
        assert(false);
#endif
    } else {
        if (bufferGATHER)
            GPUfree(bufferGATHER, id, 0, s.ptr());
        //cudaFreeAsync(bufferGATHER, s.ptr());
    }
    limb.clear();
    SPECIALlimb.clear();
    DECOMPlimb.clear();
    DIGITlimb.clear();
}

Global::Globals* LimbPartition::getGlobals() {
    return cc.precom.globals->globals[id];
}

void LimbPartition::generate(std::vector<LimbRecord>& records, std::vector<LimbImpl>& limbs, VectorGPU<void*>& ptrs,
                             int pos, VectorGPU<void*>* auxptrs, uint64_t* buffer, size_t offset, uint64_t* buffer_aux,

----- /root/fideslib/src/CKKS/LimbPartition.cu:901 -----
    cudaSetDevice(device);
    if ((!(iskey || cc.GPUid.size() == 1) && bufferGATHER == nullptr) ||
        ((iskey || cc.GPUid.size() == 1) && DECOMPlimb[0].size() == 0)) {
        int decomp_limbs = 0;
        for (auto& d : DECOMPmeta)
            decomp_limbs += d.size();
        int digit_limbs = 0;
        for (auto& d : DIGITmeta)
            digit_limbs += d.size();

        size_t size = cc.N * (/*decomp_limbs +*/ digit_limbs);
        if (cc.GPUid.size() == 1 || iskey) {
            //bufferDECOMPandDIGIT = (uint64_t*)GPUmalloc(device, std::max(1ul, size) * sizeof(uint64_t), s.ptr());
            //cudaMallocAsync(&bufferDECOMPandDIGIT, std::max(1ul, size) * sizeof(uint64_t), s.ptr());
        } else {
            // TODO do not do for key switching keys

#ifdef NCCL
            /*
            cudaStreamSynchronize(s.ptr());
            NCCLCHECK(ncclMemAlloc((void**)&bufferDECOMPandDIGIT, std::max(1ul, size) * sizeof(uint64_t)));
            NCCLCHECK(ncclCommRegister(rank, bufferDECOMPandDIGIT, std::max(1ul, size) * sizeof(uint64_t),
                                       &bufferDECOMPandDIGIT_handle));
            if (bufferDECOMPandDIGIT_handle == nullptr)
                bufferDECOMPandDIGIT_handle = (void*)-1;
            cudaDeviceSynchronize();
            */
#else
            assert(false);
#endif
        }
        //generateAllDecompLimb(bufferDECOMPandDIGIT, 0);
        generateGatherLimb(iskey);
        DECOMPlimb.resize(DECOMPmeta.size());
        for (size_t i = 0; i < DECOMPmeta.size(); ++i) {
            for (size_t j = 0; j < DECOMPmeta.at(i).size(); ++j) {
                int pos = 0;
                for (size_t k = 0; k < cc.meta.size(); ++k) {
                    for (size_t l = 0; l < cc.meta[k].size(); ++l) {
                        if (cc.meta[k][l].id == DECOMPmeta[i][j].id) {
                            generate(DECOMPmeta[i], DECOMPlimb[i], DECOMPlimbptr[i], (int)j, nullptr, bufferGATHER,

----- /root/fideslib/src/CKKS/LimbPartition.cu:902 -----
    if ((!(iskey || cc.GPUid.size() == 1) && bufferGATHER == nullptr) ||
        ((iskey || cc.GPUid.size() == 1) && DECOMPlimb[0].size() == 0)) {
        int decomp_limbs = 0;
        for (auto& d : DECOMPmeta)
            decomp_limbs += d.size();
        int digit_limbs = 0;
        for (auto& d : DIGITmeta)
            digit_limbs += d.size();

        size_t size = cc.N * (/*decomp_limbs +*/ digit_limbs);
        if (cc.GPUid.size() == 1 || iskey) {
            //bufferDECOMPandDIGIT = (uint64_t*)GPUmalloc(device, std::max(1ul, size) * sizeof(uint64_t), s.ptr());
            //cudaMallocAsync(&bufferDECOMPandDIGIT, std::max(1ul, size) * sizeof(uint64_t), s.ptr());
        } else {
            // TODO do not do for key switching keys

#ifdef NCCL
            /*
            cudaStreamSynchronize(s.ptr());
            NCCLCHECK(ncclMemAlloc((void**)&bufferDECOMPandDIGIT, std::max(1ul, size) * sizeof(uint64_t)));
            NCCLCHECK(ncclCommRegister(rank, bufferDECOMPandDIGIT, std::max(1ul, size) * sizeof(uint64_t),
                                       &bufferDECOMPandDIGIT_handle));
            if (bufferDECOMPandDIGIT_handle == nullptr)
                bufferDECOMPandDIGIT_handle = (void*)-1;
            cudaDeviceSynchronize();
            */
#else
            assert(false);
#endif
        }
        //generateAllDecompLimb(bufferDECOMPandDIGIT, 0);
        generateGatherLimb(iskey);
        DECOMPlimb.resize(DECOMPmeta.size());
        for (size_t i = 0; i < DECOMPmeta.size(); ++i) {
            for (size_t j = 0; j < DECOMPmeta.at(i).size(); ++j) {
                int pos = 0;
                for (size_t k = 0; k < cc.meta.size(); ++k) {
                    for (size_t l = 0; l < cc.meta[k].size(); ++l) {
                        if (cc.meta[k][l].id == DECOMPmeta[i][j].id) {
                            generate(DECOMPmeta[i], DECOMPlimb[i], DECOMPlimbptr[i], (int)j, nullptr, bufferGATHER,
                                     pos * cc.N, nullptr, 0, true);

----- /root/fideslib/src/CKKS/LimbPartition.cu:2653 -----
                int a = 0;

                for (size_t i = 0; i < DECOMPmeta.size(); ++i) {
                    for (size_t j = 0; j < DECOMPmeta[i].size(); ++j) {

                        void* ptr;
                        SWITCH_RET(DECOMPlimb[i][j], v.data, ptr);
                        h_gatherptr[a] = ptr;
                        a++;
                    }
                }

                if (GATHERptr.size * sizeof(void*) > 0) {
                    cudaMemcpyAsync(GATHERptr.data, h_gatherptr.data(), GATHERptr.size * sizeof(void*),
                                    cudaMemcpyHostToDevice, s.ptr());
                }
            }
        } else {
#ifdef NCCL
            cudaStreamSynchronize(s.ptr());
            NCCLCHECK(ncclMemAlloc((void**)&bufferGATHER, std::max(1ul, GATHERmeta.size() * sizeof(uint64_t) * cc.N)));
            NCCLCHECK(ncclCommRegister(rank, bufferGATHER, std::max(1ul, GATHERmeta.size() * sizeof(uint64_t) * cc.N),
                                       &bufferGATHER_handle));
            if (bufferGATHER_handle == nullptr)
                bufferGATHER_handle = (void*)-1;
            cudaDeviceSynchronize();
#else
            assert(false);
#endif
            std::vector<void*> h_gatherptr(GATHERptr.size, nullptr);
            for (size_t i = 0; i < h_gatherptr.size(); ++i)
                h_gatherptr[i] = (void*)(bufferGATHER + cc.N * i);

            if (GATHERptr.size * sizeof(void*) > 0) {
                cudaMemcpyAsync(GATHERptr.data, h_gatherptr.data(), GATHERptr.size * sizeof(void*),
                                cudaMemcpyHostToDevice, s.ptr());
            }
        }
    }
}


----- /root/fideslib/src/CKKS/LimbPartition.cu:2654 -----

                for (size_t i = 0; i < DECOMPmeta.size(); ++i) {
                    for (size_t j = 0; j < DECOMPmeta[i].size(); ++j) {

                        void* ptr;
                        SWITCH_RET(DECOMPlimb[i][j], v.data, ptr);
                        h_gatherptr[a] = ptr;
                        a++;
                    }
                }

                if (GATHERptr.size * sizeof(void*) > 0) {
                    cudaMemcpyAsync(GATHERptr.data, h_gatherptr.data(), GATHERptr.size * sizeof(void*),
                                    cudaMemcpyHostToDevice, s.ptr());
                }
            }
        } else {
#ifdef NCCL
            cudaStreamSynchronize(s.ptr());
            NCCLCHECK(ncclMemAlloc((void**)&bufferGATHER, std::max(1ul, GATHERmeta.size() * sizeof(uint64_t) * cc.N)));
            NCCLCHECK(ncclCommRegister(rank, bufferGATHER, std::max(1ul, GATHERmeta.size() * sizeof(uint64_t) * cc.N),
                                       &bufferGATHER_handle));
            if (bufferGATHER_handle == nullptr)
                bufferGATHER_handle = (void*)-1;
            cudaDeviceSynchronize();
#else
            assert(false);
#endif
            std::vector<void*> h_gatherptr(GATHERptr.size, nullptr);
            for (size_t i = 0; i < h_gatherptr.size(); ++i)
                h_gatherptr[i] = (void*)(bufferGATHER + cc.N * i);

            if (GATHERptr.size * sizeof(void*) > 0) {
                cudaMemcpyAsync(GATHERptr.data, h_gatherptr.data(), GATHERptr.size * sizeof(void*),
                                cudaMemcpyHostToDevice, s.ptr());
            }
        }
    }
}

// namespace FIDESlib::CKKS

---------------- FILE: /root/fideslib/src/PeerUtils.cu ----------------

----- /root/fideslib/src/PeerUtils.cu:67 -----
         * and exchange data. A cluster of blocks are guaranteed to be
         * concurrently scheduled onto a group of SMs.
         * The maximum value is 8 and it must be divisible into the grid dimensions
         */
    /*
    CUlaunchConfig launchConfig = {0};
    CUlaunchAttribute launchAttrs[3];
    int attrs = 0;
        if (clusterSize) {
            // Grid dimension must be divisible by clusterSize
            if (grid.x % clusterSize) clusterSize = 1;
            launchAttrs[attrs].id = CU_LAUNCH_ATTRIBUTE_CLUSTER_DIMENSION;
            launchAttrs[attrs++].value.clusterDim = {clusterSize, 1, 1};
            launchAttrs[attrs].id = CU_LAUNCH_ATTRIBUTE_CLUSTER_SCHEDULING_POLICY_PREFERENCE;
            launchAttrs[attrs++].value.clusterSchedulingPolicyPreference = CU_CLUSTER_SCHEDULING_POLICY_SPREAD;
        }
#if CUDART_VERSION >= 12000
        if (compCap >= 90 && driverVersion >= 12000) {
            // Set the NCCL Mem Sync domain on CUDA 12.0 and later (sm90)
            launchAttrs[attrs].id = CU_LAUNCH_ATTRIBUTE_MEM_SYNC_DOMAIN;
            launchAttrs[attrs++].value.memSyncDomain = (CUlaunchMemSyncDomain) ncclParamMemSyncDomain();
        }
#endif
*/

    CUlaunchConfig config = {
        .gridDimX = grid.x,
        .gridDimY = grid.y,
        .gridDimZ = grid.z,
        .blockDimX = block.x,
        .blockDimY = block.y,
        .blockDimZ = block.z,
        .sharedMemBytes = 0,  // grid, block, shared memory
        .hStream = s,         // stream
        .attrs = attr,
        .numAttrs = sizeof(attr) / sizeof(CUlaunchAttribute),
    };

    // This bypasses some capture restrictions
    CUresult launchResult = cuLaunchKernelEx(&config, kernel, args /*nullptr*/, nullptr /*extra*/);
    //CUresult launchResult = cuLaunchKernelExC(&config, &p2p_polling_kernel, args);

----- /root/fideslib/src/PeerUtils.cu:776 -----
        memcpyParams.srcArray = NULL;
        memcpyParams.srcPos = make_cudaPos(0, 0, 0);
        memcpyParams.srcPtr = make_cudaPitchedPtr(src, elems * sizeof(float), elems * sizeof(float), 1);

        memcpyParams.dstArray = NULL;
        memcpyParams.dstPos = make_cudaPos(0, 0, 0);
        memcpyParams.dstPtr = make_cudaPitchedPtr(dst, elems * sizeof(float), elems * sizeof(float), 1);
        memcpyParams.extent = make_cudaExtent(elems * sizeof(float), 1, 1);
        memcpyParams.kind = cudaMemcpyDefault;

        cudaGraphAddMemcpyNode(&copy_0to1, _capturing_graph, _deps, _dep_count, &memcpyParams);

        cudaStreamUpdateCaptureDependencies(s, &copy_0to1, 1, 1);

    } else if (!is_stream_being_captured(s)) {
        // p2p_transfer_1d<<<involved_sm, 128, 0, s>>>(src, dst, elems);

        cudaPointerAttributes attr_src, attr_dst;
        cudaPointerGetAttributes(&attr_src, src);
        cudaPointerGetAttributes(&attr_dst, dst);
        cudaMemcpyPeerAsync(dst, attr_dst.device, src, attr_src.device, elems * sizeof(float), s);
    } else if (1) {

        CudaCheckErrorModNoSync;
        void* args[] = {&src, &dst, &elems};

        cudaFunction_t kernel;
        CudaCheckErrorModNoSync;
        cudaGetFuncBySymbol(&kernel, (const void*)&p2p_transfer_1d);
        CudaCheckErrorModNoSync;

        launch(kernel, involved_sm, 128, args, 3, s);
        CudaCheckErrorModNoSync;
    } else {
        // Get the current capturing graph context
        cudaStreamCaptureStatus capture_status;
        cudaGraph_t capturing_graph;
        const cudaGraphNode_t* deps;
        size_t dep_count;

        cudaStreamGetCaptureInfo(s, &capture_status, nullptr, &capturing_graph, &deps, &dep_count);
